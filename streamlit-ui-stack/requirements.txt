streamlit>=1.39,<2
bcrypt>=4.2
httpx>=0.27
python-multipart>=0.0.9
# Card 4 (Run Playbook) parses playbook YAML files at /ansible/playbooks to
# show their `name:` and `description:` in the dropdown tooltip. Pure-Python
# implementation; ~700KB wheel on python:3.12-slim.
PyYAML>=6.0
