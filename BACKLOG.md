# AIAMSBS Backlog

## Planned Improvements

### High Priority

| # | Item | Description | Complexity |
|---|------|-------------|-------------|
| 1 | Pre-provisioned Grafana dashboards | **[RESOLVED 2026-06-28 — see commit b032ef5.](https://github.com/rylanddufour/AIAMSBS/commit/b032ef5)** Removed the 3 broken pre-bundled Grafana dashboards (`docker-logs.json`, `docker-monitoring.json`, `linux-host-overview.json`) from the repo. They shipped with empty `title` fields and failed to load in Grafana with "Dashboard title cannot be empty", producing ~3 provisioning errors per reload cycle (every 10s) per file. The `network-syslog.json` (Network Device Logs, uid `network-syslog`, 2 Loki panels) was NOT broken and is preserved. The Health check dashboard (#2) and the Network Device Logs dashboard are the only auto-provisioned dashboards going forward. Customers can build their own via the Grafana UI or import JSON through the API. | — |
| 2 | Health check dashboard | **[RESOLVED — commit 7385d7b](https://github.com/rylanddufour/AIAMSBS/commit/7385d7b), 2026-06-28.** Added `config/grafana/provisioning/dashboards/health-check.json` (uid `aiamsbs-health`, 11 panels, 30s refresh) auto-provisioned by Grafana's existing dashboards.yml. Rows: overview stats (services up, prom series, loki 5m lines), per-service status (5 prom jobs + Loki sources), Service Detail table (color-coded up/scrape-age), Live Logs panel (docker + syslog). E2E-verified on VM 220. Subsequent layout/threshold fixes: `42a8b3a` (panel #14 query + logs h=30 + thresholds), `d369cdd` (timeseries thresholds aligned yellow@80/red@90), `4a95f6e` (Host panel h=4), `dc469eb` (blank panel #71 at x=16,y=2). | Low |
| 3 | Default alerting rules | **[RESOLVED — commit 403782c](https://github.com/rylanddufour/AIAMSBS/commit/403782c), 2026-07-12 (merged to main 833bc83).** Phase 1: 3 customer-server alert rules (`cust-srv-host-down`, `cust-srv-disk-low`, `cust-srv-disk-fill-soon`) merged into the `customer-network` group in `config/grafana/provisioning/alerting/customer-network.yml`. Workaround for Grafana 13 `ngalert.scheduler` bug where rules in a 2nd folder/group register but never tick — all 11 rules now share one group, one folder, one scheduler registration. linux_exporter / windows_exporter scrape jobs ship **empty on day 0 by design** (no customer inventory yet); BACKLOG #43 will register targets as inventory is learned — framework is in place to accommodate. Per-host alert semantics (no `count()`/`sum()` aggregates, threshold refs reduce output so `labels.host`/`labels.instance` propagate). Re-split into separate group pending upstream scheduler fix (separate follow-up). | Low |

### Medium Priority

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 10 | ~~Add hostname label to Alloy metrics~~ | **[RESOLVED — PR #29 (commit 41e4119)](https://github.com/rylanddufour/AIAMSBS/commit/41e4119), 2026-07-17.** `config/alloy.yml` uses `replacement = sys.env("HOSTNAME")` to stamp the host label on every metric; health dashboard correctly queries `label_values(up{job="integrations/unix"}, host)`. Solved natively by #44 (Alloy migration). | — |
| 28 | Tiered LLM model recommendations for IT_ADMIN profile | Research completed 2026-06-29 (kanban `t_24a9f2e8`, output `/tmp/research_output.md`, 235 lines). **Tiered picks:**<br>• GOOD — Claude Sonnet 4.6 (Anthropic): $3/$15 per 1M, 1M ctx, most reliable MCP tool-calling at 64+ tools<br>• BETTER — Gemini 3.5 Flash (Google): $1.50/$9 per 1M, 1M ctx, cheapest 1M-ctx option<br>• BEST — Claude Opus 4.8 (Anthropic): $5/$25 per 1M, 1M ctx, best diagnostic reasoning<br><br>**Headline:** current AIAMSBS default (MiniMax M2.5) is NOT recommended for production — tool-calling unreliable at 64+ MCP tools, savings don't justify broken diagnostics.<br><br>**Avoid:** Claude Fable 5 (export restrictions), GPT-4o (deprecated), preview models (no SLA), MiniMax M2.5 for production (tool-calling failure rate).<br><br>**Monthly cost @ ~500 queries/day for small shop (1-10 people):**<br>• GOOD (Sonnet): $80-150<br>• BETTER (Flash): $45-80<br>• BEST (Opus): $150-300<br>• CURRENT (MiniMax): $3-6 (false economy)<br>• LOCAL (Llama 4 Scout via Ollama): hardware only, 30-50% fewer successful tool calls (compliance pick, not performance)<br><br>**Follow-up work:** (a) per-provider API-key prompt in `bootstrap.sh` (currently only sets `OPENROUTER_API_KEY`; needs `ANTHROPIC_API_KEY` / `GOOGLE_API_KEY` / `OPENAI_API_KEY` paths); (b) make `model.default` + `model.provider` configurable per-install (currently hardcoded to MiniMax via OpenRouter in `bootstrap.sh` + `register_inventory_mcp` etc.). (c) Local/Ollama path needs hardware requirement docs + setup script.<br><br>**Decision pending Ryland:** which tier to ship as AIAMSBS default. Recommended: Sonnet 4.6 (GOOD) — Anthropic is most stable (99.8%+ uptime), tool-calling gold standard, prompt caching cuts repeat-context 60-80%. | Medium |
| 27 | ~~AIAMSBS host logs in health dashboard (kanban: t_79dcbaae)~~ | **[RESOLVED — covered by #2 (commit 7385d7b)](https://github.com/rylanddufour/AIAMSBS/commit/7385d7b), 2026-06-28.** Live Logs panel in `health-check.json` queries `{source="aiamsbs_host"}`, which includes host-level logs (kernel, systemd, auth, sshd, etc.) emitted by Alloy's `loki.source.journal` block. **Caveat:** the granular noise filter from the original spec (`priority=err\|warning`, `sshd\|sudo\|auth\|kernel\|OOM` substring) was not built — current panel shows everything from the AIAMSBS host alloy source rather than a filtered "security events" subset. Spec was overspecified; functional outcome met. | — |
| 29 | AI Agent activity dashboard (kanban: t_002a166f) | Hermes currently exposes no unified view of "what are my agents doing?" Operators need to see prompts flowing through profiles, cron jobs running, kanban cards dispatching, profile health, tool calls, token usage, and errors — all in one Grafana dashboard. The data sources exist (`~/.hermes/sessions/` SQLite for messages, `kanban.db` for tasks/runs, `cron/` for job logs, `gateway.log` for traffic) but aren't aggregated. Build a Prometheus exporter + Loki pipeline + Grafana dashboard that surfaces agent activity alongside the existing platform observability.<br><br>**Data sources available today:**<br>• `~/.hermes/sessions/` (SQLite) — session messages, prompts, responses, tool calls, timestamps, token counts (if Hermes records them)<br>• `~/.hermes/kanban/boards/*/kanban.db` — tasks, runs, heartbeats, events, run outcomes<br>• `~/.hermes/cron/` — cron job logs (stdout/stderr)<br>• `~/.hermes/gateway.log` — Telegram/web traffic to/from agents<br>• `~/.hermes/profiles/<name>/` — per-profile config, memory, skills<br>• Existing Prometheus + Loki + Alloy plumbing (just add new scrape jobs + pipeline stages)<br><br>**Mechanisms:**<br>• **Prometheus exporter** (`services/agent-exporter/`) — small Python service that reads Hermes session/kanban/cron DBs and exposes `/metrics` on a port (suggest 9117). Counters (`prompts_total{profile}`), gauges (`active_sessions{profile}`, `last_activity_timestamp{profile}`), histograms (`turn_duration_seconds{profile}`)<br>• **Loki pipeline** — wire session DB rows (prompts/responses) to Loki as logfmt with `profile`, `session_id`, `direction`, `tool_count` labels. New stream: `job=hermes-sessions`. Extend Promtail/Alloy config or add a new pipeline stage<br>• **Cron → Loki** — capture cron stdout/stderr to Loki. New stream: `job=hermes-cron`<br>• **Kanban events → Loki** — tail kanban events table, emit on transitions. New stream: `job=hermes-kanban`<br>• **Grafana dashboard** — `config/grafana/provisioning/dashboards/agent-activity.json` (provisioned like `health-check.json`)<br><br>**Metrics (proposed):**<br>• *Overview:* `active_profiles`, `prompts_24h_total`, `cards_completed_24h`, `error_rate_24h`<br>• *Per-profile:* `last_activity_timestamp{profile}`, `active_sessions{profile}`<br>• *Volume:* `prompts_per_hour{profile}`, `tool_calls_per_hour{profile,tool}`, `subagent_delegations_per_hour{profile}`<br>• *Tokens:* `tokens_used_total{profile,direction}`, `avg_tokens_per_turn{profile}`<br>• *Cron/kanban:* `cron_job_last_status{name}`, `cron_job_last_duration_seconds{name}`, `kanban_runs_in_flight`, `kanban_failed_runs_24h`<br>• *Reliability:* `crashes_24h{profile}`, `timeouts_24h{profile}`, `mcp_errors_24h{server}`<br><br>**Loki streams (proposed labels):**<br>• `job=hermes-sessions` — every prompt/response line with `profile`, `session_id`, `direction` (`in`/`out`), `tool_count`<br>• `job=hermes-cron` — cron stdout/stderr with `job_name`, `profile`<br>• `job=hermes-kanban` — kanban events with `event_type`, `card_id`, `profile`<br><br>**Dashboard rows (proposed):**<br>• Row 1: Overview stats (4 tiles — active profiles, prompts 24h, cards done 24h, error rate)<br>• Row 2: Per-profile health (stat tiles with last activity timestamp, color-coded stale)<br>• Row 3: Activity volume (time series: prompts/hr per profile, tool calls/hr stacked, delegations/hr)<br>• Row 4: Token usage (stat tiles for 1h/24h/7d + time series)<br>• Row 5: Cron & kanban (status table for cron jobs + stat for runs-in-flight + last kanban events)<br>• Row 6: Live logs panels (last prompts per profile, recent errors, recent kanban events)<br>• Row 7: Reliability (crash count, timeout count, MCP error rate)<br><br>**Privacy/security:**<br>• Prompts may contain secrets (API keys, passwords, JWT tokens). Add a **redaction step** before Loki ingestion — regex patterns for known token shapes (`sk-...`, JWT regex, env var references, generic API key patterns)<br>• Optional opt-out: `prompts_in_loki: false` flag in `bootstrap.sh` for operators who don't want prompt content retained<br>• Shorter retention on `hermes-sessions` stream (default 24h–7d, configurable) vs. metrics retention<br>• Consider stripping PII fields before logging (operator names, customer names from inventory)<br><br>**Related / in flight:**<br>• #2 (Health check dashboard) RESOLVED — #29 is additive, doesn't replace<br>• #9 (Metrics for Hermes itself) is a related **pre-req** — baseline Prometheus metrics for the Hermes process should land first so the agent-exporter doesn't compete for the same `/metrics` namespace<br>• #27 (Host logs in health dashboard) — host system logs, not agent logs; separate item<br>• #28 (Tiered LLM model recommendations) — different problem (cost optimization); #29 is visibility<br>• #5 (Loki retention) — may need to bump retention for `hermes-sessions` stream specifically<br><br>**Out of scope (park as separate items if surfaced):**<br>• Token **cost** attribution per profile (cost tracking is v1.1)<br>• Alerting on agent anomalies (e.g., "profile X silent for 24h", "MCP server error rate spike")<br>• Cross-profile comparison views ("is IT_ADMIN more active than default?")<br>• Per-tool latency histograms (needs Hermes-side instrumentation to capture)<br>• Historical retention beyond 7d for prompt content (separate retention config item)<br>• Agent-side recording of reasoning traces (only prompts/responses are captured by default)<br><br>**Acceptance:**<br>• Dashboard renders with all rows visible, all metrics backed by real data (no "No data" panels)<br>• Test prompts appear in the logs panel with correct profile labels within 60s of submission<br>• Redaction tested: synthetic prompt with fake `sk-...` API key — key does NOT appear in Loki<br>• Smoke test on VM 220: trigger 5 prompts across profiles → all metrics increment, all dashboards refresh | Medium |
| 35 | Multi-host observability: promtail per-port + node_exporter dashboard (kanban: t_df72bab6) | AIAMSBS observability is currently scoped to the host it runs on. Customer environments are larger (1-10 IT staff serving many more devices/servers) and need a **per-server dashboard** for every `node_exporter`-equipped host, with the AIAMSBS host itself as just one of N entries in the dropdown.<br><br>**Scope (delivered on branch `wt/multi-host-observability-20260705`, 7 commits):**<br>• Promtail per-port syslog receivers: `514` (network devices, TCP+UDP), `1514` (customer Linux, TCP), `2514` (customer Windows, TCP) — each with explicit `source` label (`network_device`, `customer_host_linux`, `customer_host_windows`)<br>• Docker compose: port mappings for the new listeners; UDP added to 514 for network gear<br>• Prometheus: new `node_exporter` job with `honor_labels: true` and per-target `host` label (seeded with `aiamsbs-host` for 192.168.0.220:9100); alloy metrics get a static `host: aiamsbs-host` relabel so the dropdown has at least one entry before node_exporter is installed on 220 (BACKLOG #10 / #B partially resolved)<br>• Grafana: new `aiamsbs-node-exporter` dashboard (uid `aiamsbs-node-exporter`) with a `$host` variable (`label_values(node_uname_info, host)`) and 23 content panels (Identity, CPU, Memory, Disk, Network, System, Logs rows); existing `aiamsbs-health` dashboard gets a new "Promtail Listeners" row with active-connections, parse-errors, traffic-bytes timeseries and per-port up stat tiles<br>• Docs: `docs/multi-host-monitoring.md` (412 lines) — Linux + Windows `node_exporter` install, rsyslog/NxLog client config, Prometheus target registration, verification checklist, troubleshooting<br><br>**Port convention (locked in `config/promtail.yml`):** `514` = network devices, `1514` = customer Linux, `2514` = customer Windows. Do not change without updating this row.<br><br>**Known pre-existing inconsistency (NOT in scope for this card, tracked separately):** `docker-compose.yml` still maps `0.0.0.0:1514:514/tcp` (host 1514 → container 514), but promtail's `customer_host_linux` scrape_config binds to `0.0.0.0:1514` inside the container, so customer Linux syslog traffic is being misrouted to the `network_syslog` listener. Follow-up: change the mapping to `0.0.0.0:1514:1514/tcp`.<br><br>**E2E:** deferred to Ryland on VM 220 (requires building a Windows server in his lab to test the 2514 + Windows node_exporter path). | Medium |
| 37 | snmp_exporter for network device monitoring | AIAMSBS targets small IT shops with network gear (BACKLOG #12 lists **Cisco Catalyst**, **Ubiquiti UniFi**, **Aruba Networks** as in-scope OEMs). Today the only network-gear telemetry AIAMSBS pulls is **syslog** (Promtail listener on :514, see BACKLOG #35). For interface counters, BGP/OSPF neighbor state, PoE draw, wireless client counts, switch port errors, etc., we need **SNMP polling**.<br><br>**Use:** [prometheus/snmp_exporter](https://github.com/prometheus/snmp_exporter) (`prom/snmp_exporter`, latest stable). It walks the SNMP tree per a `snmp.yml` config (auth, modules, which OIDs to scrape) and exposes Prometheus metrics. AIAMSBS customers don't run an agent on the switch — the exporter is the central poller.<br><br>**Scope:**<br>• **Compose:** add `snmp_exporter` service to `docker-compose.yml` (`prom/snmp-exporter:latest`, `network_mode: host` so it can reach switches on the LAN, port `9116`, volume-mount a default `config/snmp.yml`)<br>• **Prometheus:** new `snmp_exporter` scrape job with `honor_labels: true` and per-target `host` static label (e.g., `host: sw-core-01` for `192.168.0.2`); targets grouped by OEM module (Cisco / Aruba / generic)<br>• **snmp.yml:** seed with the common AIAMSBS modules — `if_mib` (interface counters, the most-asked-for metric), `cisco_c2960` (or generic Cisco), `aruba` (or generic), plus the default `snmp` (sysName, sysUpTime, etc.). Customer can extend on the box.<br>• **Grafana:** new `aiamsbs-network-devices` dashboard with `$host` dropdown (`label_values(snmp_sysUpTime, host)`), per-device rows for: interface utilization (top-N from `if_mib`), sysUpTime, neighbor count, error counters, PoE draw (Aruba-specific). Reuse the `aiamsbs-node-exporter` Identity-row pattern (Up / OS / Uptime / hostname-style stats) for visual consistency<br>• **Hermes skill:** extend `it_admin/skills/` with a `network-device.md` (and per-OEM sub-skills per BACKLOG #12) covering how to add a new device to snmp.yml, what credentials look like, what to check if a device goes dark<br>• **Docs:** add a "Network device monitoring" section to `docs/multi-host-monitoring.md` (which already covers node_exporter + Promtail — extend, don't fork)<br><br>**Auth / credentials:** SNMPv2c community string by default (v3 needs per-device securityNames + authPass phrases, significantly more config). Community string should live in `~/.hermes/secrets/snmp-exporter.env` (same pattern as the Grafana SA token for #6's backup), env-var interpolated into snmp.yml. Document the rotation flow.<br><br>**Related / in flight:**<br>• BACKLOG #12 (Multi-OEM skills) — snmp_exporter is the observability backbone for the Cisco / UniFi / Aruba work. Pick whichever comes first; they unblock each other.<br>• BACKLOG #11 (Test syslog with real network device) — already done (2026-06-30, RESOLVED). snmp_exporter is the metrics complement to that syslog work.<br>• BACKLOG #35 (Multi-host observability) — the snmp_exporter job fits the same per-`host` label + dashboard pattern; reuse, don't reinvent.<br>• The `network-syslog` Promtail listener (BACKLOG #35) and snmp_exporter both target the same devices — one source for syslog, one for metrics. Document the two paths so a customer doesn't double-up or gap.<br><br>**Out of scope:**<br>• NetFlow / IPFIX / sFlow — different exporter entirely (e.g., `pmacctd` or `go-netflow`). Add separately if a customer asks.<br>• SNMP trap reception — not in the snmp_exporter's scope. Use snmptrapd + Loki pipeline if a customer needs traps.<br>• Per-OEM advanced features (e.g., Cisco Meraki, Aruba Central cloud API) — cloud-managed gear has its own API, separate track.<br>• Alert rules for network devices — own BACKLOG item once the dashboard exists and we know what the customer actually alerts on. | Medium |
| 38 | vCenter OTel receiver for VMware vSphere monitoring | AIAMSBS targets VMware vSphere (BACKLOG #12). Today **zero vSphere telemetry** flows to AIAMSBS — no host CPU/mem, no VM inventory, no datastore capacity, no vMotion events. This is a gap for any customer running ESXi/vCenter, which is most of the small-shop target market.<br><br>**Use:** Grafana Agent (alloy) has a built-in OpenTelemetry component — [`otelcol.receiver.vcenter`](https://grafana.com/docs/agent/latest/flow/reference/components/otelcol.receiver.vcenter/). It connects to vCenter/ESXi over the vSphere API and emits OTel metrics on the standard Prometheus pipeline. **No new container needed** — alloy is already running (BACKLOG #29-ish work added the host metrics side).<br><br>**Scope:**<br>• **alloy config (`config/alloy.yml`):** add a `otelcol.receiver.vcenter "vcenter" {` block with `endpoints = ["https://<vcenter-host>/sdk"]`, `username` + `password` from env (`VCENTER_USER` / `VCENTER_PASSWORD`), `interval = "30s"`. Wire its output into the existing Prometheus pipeline via `otelcol.exporter.prometheus` → remote_write (matches the host-metrics pattern).<br>• **Secrets:** `~/.hermes/secrets/vcenter-exporter.env` with `VCENTER_USER=...` + `VCENTER_PASSWORD=...`. Same pattern as the Grafana SA token. Document rotation.<br>• **Prometheus:** confirm `up{job="otelcol.receiver.vcenter"}` and the standard ESXi/VM metrics (`vcenter_host_cpu_usage_mhz`, `vcenter_vm_memory_used_mib`, `vcenter_datastore_disk_used_latest`, `vcenter_vm_count`, etc.) — adjust job label regex in the existing scrape config if needed.<br>• **Grafana:** new `aiamsbs-vsphere` dashboard (or a new "Virtualization" row on the existing `aiamsbs-health` dashboard) with: host CPU/mem (top-N), VM inventory count + status, datastore capacity gauges, vMotion events (Loki query), per-host network throughput<br>• **Hermes skill:** `it_admin/skills/vsphere.md` — how to add a vCenter, what to check if metrics go stale, how to map a `vcenter_host` to a `host` label for the dashboard dropdown (the AIAMSBS host label vs the vcenter host name are usually the same on small shops, but the connector must be aware)<br>• **Docs:** add a "vSphere monitoring" section to `docs/multi-host-monitoring.md`<br><br>**Auth / connection:** the vSphere API is a SOAP/XML thing over HTTPS on port 443 (or 5480 for standalone ESXi). Need read-only vCenter credentials, plus a vCenter user with `Read-only` role at the vCenter level (not per-ESXi). Document the minimum privilege.<br><br>**Related / in flight:**<br>• BACKLOG #12 (Multi-OEM skills) — vSphere is one of the 6 OEMs. This card is the metrics mechanism; the skill work in #12 is the agent-side.<br>• BACKLOG #10 / #B (hostname label) — the `host` label convention established for node_exporter / alloy should apply to vcenter data too, so a single `label_values(up, host)` works across the whole stack.<br>• BACKLOG #35 (Multi-host observability) — same per-`host` dashboard pattern.<br>• BACKLOG #29 (AI Agent activity dashboard) — different scope (Hermes self-monitoring). Not related.<br><br>**Out of scope:**<br>• vSphere event/log ingestion (tasks, alarms, hostd log) — vCenter has its own syslog feed; use Promtail listener (per BACKLOG #35) if a customer wants it. OTel receiver is metrics-only.<br>• VM-level deep metrics (per-VM CPU, per-VM disk IO) — the OTel receiver emits host + VM + datastore + cluster metrics, but not per-VM series at the same cardinality as `windows_exporter` on a Windows VM. If a customer needs per-VM monitoring, deploy `node_exporter` inside the guest (BACKLOG #35 pattern).<br>• vSphere tags / custom attributes — surfaced separately if needed.<br>• vSAN metrics — different API; out of scope unless a customer asks. | Medium |
| 44 | ~~Migrate customer hosts from (node_exporter + Promtail + rsyslog/NxLog) to Alloy~~ | **[RESOLVED — PR #35 (commit 46b7c96)](https://github.com/rylanddufour/AIAMSBS/pull/35), 2026-07-19.** All 11 in-scope steps landed: (1) Linux River `b626841` (#22), (2) Windows River `378f2d4` (#24), (3) Linux pilot `fa4b4e0` (#25), (4) parity verify `4d4500a`, (5/6/7) Linux+Windows cutover closed via `5c9e917`, (7a) dashboard relabel `55ad740` (#27), (9) docs `55d59a7` (#33), (10) Promtail :1514+:2514 decommission `acfb2cc` (#34), (11) Promtail Listeners row removal `46b7c96` (#35). Step 8 (`bootstrap.sh install_alloy_customer_host()`) dropped per Ryland 2026-07-18 — AIAMSBS does not install on customer devices. Customer hosts reduced from 3 processes (node_exporter + Promtail + rsyslog/NxLog) to 1 (Alloy). Per-host dashboard dropdown (`$host`) works via `label_values(up{job="integrations/unix"}, host)`. **Note:** `wt/alloy-steps-admin-closeout-20260719` worktree remains as the final BACKLOG-text admin close-out (`5c9e917`); no functional code pending. | — |

**Mandatory verification gates — pass at every check (parity verify / step 4, pre-cutover steps 6+7, post-cutover, post-Promtail-decommission / step 10):** added 2026-07-17 after a Network Device Logs dashboard outage surfaced during the Win11 Alloy pilot (BACKLOG #44 step 5) — the promtail pipeline and its dependent dashboard can drift silently under the migration; the only reliable signal is a fresh end-to-end check at each gate. **Both checks must pass before the gate closes — any failure stops the migration at that step, no skipping.**
• **Network Device Logs dashboard:** open it in Grafana. Host dropdown has ≥1 entry (label `label_values({source="network_device"}, host)` returns non-empty in Prom/Loki). "Recent Logs" panel shows entries newer than the gate's start time. If empty: do not advance the migration — investigate promtail receive path before continuing.
• **Promtail health (port 514 listener only — :1514/:2514 will be gone after step 10):** `docker logs promtail --since 5m` shows **zero** `error parsing syslog stream` and **zero** `401 no org id` lines. A single parse error in the window means OPNsense format detection drifted; a single 401 means the loki multi-tenant issue (#45) came back. Fix the underlying cause; do not advance.

These checks are mechanical — scriptable as a small bash snippet that runs the Prom query, runs the Loki label query, greps the promtail logs, and exits non-zero on any failure. Add that script to `scripts/verify-network-logs.sh` as part of step 11 (rollout runbook) so the gate is repeatable on every customer cutover.<br><br>**Why this matters:** solves BACKLOG #10 (hostname label) natively, unlocks BACKLOG #38 (vCenter OTel — same agent), reduces operational footprint from 3 processes per host to 1, single upgrade path for future telemetry.<br><br>**Related:** #10 (hostname label — solved by this), #27 (host logs in dashboard — alloy journal already provides), #35 (multi-host observability — current state, rewritten in Step 9), #38 (vCenter OTel — alloy already in place after this lands).<br><br>**Step 7a detail — migrate per-OS dashboards to alloy job labels:** discovered 2026-07-17 while bringing the Win11 Alloy host online — the migration as written changes the Prometheus job labels on customer hosts (`node_exporter` / `windows_exporter` → `integrations/unix` / `integrations/windows`) and the Loki log labels (`customer_host_linux` / `customer_host_windows` → `systemd` / `windows_eventlog`), but the existing per-OS Grafana dashboards still query the OLD labels. Result: at cutover (steps 6 + 7) both dashboards go blank until this step runs. Inserting 7a between step 7 and step 8 so the dashboards are correct BEFORE the installer ships.<br><br>**What breaks without 7a (verified on .220 live state):**<br>• `config/grafana/provisioning/dashboards/node-exporter.json` — Prom variable: `label_values(up{job="linux_exporter"}, host)` → dropdown empty post-cutover (alloy emits `job="integrations/unix"`). Loki panel #29: `{host="$host", job=~"customer_host_linux|customer_host_windows"}` → empty (alloy journal emits `job="systemd"`, windowsevent emits `job="windows_eventlog"`).<br>• `config/grafana/provisioning/dashboards/node-exporter-windows.json` — Prom variable: `label_values(up{job="windows_exporter"}, host)` → dropdown empty post-cutover (alloy emits `job="integrations/windows"`, confirmed live on the Win11 pilot box 2026-07-17). Loki panel #15: `{job="customer_host_windows", host="$host"}` → empty (alloy emits `job="windows_eventlog"`).<br><br>**Precedent:** `config/grafana/provisioning/dashboards/health-check.json` already runs on the post-migration labels (`job="integrations/unix"` for the AIAMSBS host's own CPU/mem/disk panels). 7a brings the per-OS dashboards in line with that pattern.<br><br>**Scope of 7a:**<br>• `node-exporter.json` — rewrite Prom variable to `label_values(up{job="integrations/unix"}, host)`; sweep every `job="linux_exporter"` panel query → `job="integrations/unix"`; update Loki panel #29 from `job=~"customer_host_linux|customer_host_windows"` to `job=~"systemd"` (or `job=~"systemd|windows_eventlog"` if a unified logs panel is wanted).<br>• `node-exporter-windows.json` — same shape: Prom variable → `label_values(up{job="integrations/windows"}, host)`; sweep `job="windows_exporter"` → `job="integrations/windows"`; Loki panel #15 → `job="windows_eventlog"`.<br>• Keep dashboard filenames + uids + panel titles unchanged so customer bookmarks + Grafana links survive.<br>• Add a small subtitle per dashboard: *"Per-OS host metrics + logs via Grafana Alloy. The AIAMSBS host itself is tracked on `aiamsbs-health`."*<br>• Verify: import the new JSON into a fresh Grafana; confirm both dashboards render non-empty against the AIAMSBS .220 + the Win11 pilot box already in place.<br><br>**Why this is a step and not part of step 9 (docs):** the dashboards are runtime config that ships inside the provisioned Grafana — if 9 lands before the dashboards are migrated, step 10 (Promtail :1514/:2514 decommission) severs the data sources for the dashboards that still query them. 7a ordering fixes that.<br><br>**Sub-task kanban:** [`t_17be167a`](https://hermes.../t_17be167a) (created 2026-07-17, unassigned). Cross-reference this row from #35 (current per-OS dashboards) and #27 (host logs in dashboard). | High |

### Multi-OEM Skills (New Capability Track)

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 12 | ~~Multi-OEM skill library~~ | **[RESOLVED — commit 983a2ba](https://github.com/rylanddufour/AIAMSBS/commit/983a2ba), 2026-07-08.** All in-scope OEMs covered by 23 skill files in `profiles/it_admin/skills/`: Windows Server (`windows-server.md`), Active Directory (`active-directory.md`), Linux (`linux-admin.md`), Cisco Catalyst IOS (`network-oem-cisco-ios.md`), Ubiquiti UniFi (`network-oem-ubiquiti-unifi.md`), Aruba Networks (`network-oem-hpe-aruba.md`), VMware vSphere (`vsphere-admin.md`), plus the `network-oem-index.md` umbrella. `bootstrap.sh install_it_admin_profile_soul()` ships them on every customer VM. **Note:** SQL Server not in original scope — it's an application/database, not a managed device. If a customer needs SQL Server coverage, it's a NEW scope item, not a #12 gap. | — |

### Release & Deployment (New Track)

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 24 | Default profile MCP auto-loading | **[RESOLVED — PR #9](https://github.com/rylanddufour/AIAMSBS/pull/9), merged 2026-06-27.** Root cause: `~/.hermes/profiles/default/config.yaml` is a **dead location** Hermes never reads — the default profile's config IS the global `~/.hermes/config.yaml`. Older `register_inventory_mcp` auto-detected multi-profile layout via `~/.hermes/profiles/` dir existence and wrote the default profile config to the dead location, silently ignored by Hermes. Fix: `register_inventory_mcp()` special-cases `profile == "default"` to always write to `~/.hermes/config.yaml` (lines ~1057-1162 of bootstrap.sh); idempotency preserved + one-shot migration strips stale `mcp_servers:` from the dead default subdir. Verified on VM 220: smoke test 14/14 PASS, default profile drives inventory via natural language. Discovered during E2E verification card `t_f4ff4c7b`. | — |
| 46 | Alert Events dashboard (v1.x feature) | Built-in Grafana dashboard for the firing event history of every Grafana-managed alert rule, plus the Loki state-history plumbing it needs. Customer-visible value: one screen answers "what's firing right now, what fired this week, and which devices are the chronic offenders over the last 30 / 180 / 365 days".<br><br>**Scope (deployed on .220 — dashboard URL `http://192.168.0.220:3000/d/aiamsbs-alert-events/alert-events`, uid `aiamsbs-alert-events`, 7 panels, 30s refresh, provisioned from `config/grafana/provisioning/dashboards/alert-events.json`):**<br>• **Top row** — `Still Open` (Prometheus `grafana_alerting_alerts{state=~"alerting\|pending\|recovering\|error\|nodata"}`) + `Firing Events — This Week` (Loki `count_over_time({from="state-history"} \| json \| current="Alerting" [7d])`). Live on .220: 3 open, 13 firing events in 7d.<br>• **Current Alerts** — Grafana native `alertlist` panel, newest first, shows live rules + instances.<br>• **Top 5 Devices** — three `bargauge` panels for `[30d] / [180d] / [365d]` rolling windows, query `sort_desc(topk(5, sum by (labels_host) (count_over_time({from="state-history"} \| json \| current="Alerting" \| labels_host!="" [window]))))`. Live: `.211`×4 leads all three windows.<br>• **Recent Firing Events** — Loki logs panel with `line_format "{{.ruleTitle}} • {{.labels_host}} • {{.labels_severity}}"` so each row reads as `CustomerDeviceUnreachable • 192.168.0.219 • critical`. Expandable for full labels.<br><br>**Plumbing required (deployed as part of this card, not separate items):**<br>• **`docker-compose.yml`** — Grafana service gets `GF_UNIFIED_ALERTING_STATE_HISTORY_ENABLED=true`, `GF_UNIFIED_ALERTING_STATE_HISTORY_BACKEND=loki`, `GF_UNIFIED_ALERTING_STATE_HISTORY_LOKI_REMOTE_URL=http://loki:3100`, `GF_FEATURE_TOGGLES_ENABLE=alertingCentralAlertHistory`. Without these, `from="state-history"` stays empty and the dashboard is blank from day 1.<br>• **`config/loki.yml`** — global retention stays at **90 d** (BACKLOG #5); new `retention_stream` with selector `{from="state-history"}` retains **1 year (8760 h)** for the alert-history stream only. Disk impact ≈ zero (alert transitions are one tiny line per state change). Also `split_queries_by_interval: 24h` + `split_instant_metric_queries_by_interval: 24h` — **required** for the 30/180/365-day queries to return correct counts (Loki's default 1 h split was returning 3 instead of 13 for the 7-day window until fixed).<br>• **Backfill** — `scripts/backfill_alert_history.py` imports existing Grafana annotations into the new Loki `state-history` stream so historical panels show real numbers on day 1 instead of starting empty. Reads `/api/annotations?type=alert&from=<now-400d>&limit=1000`, resolves IP→hostname via inventory-mcp, writes Loki push with `source: annotations-backfill`. Verified on .220: 139 historical annotations ingested, 13 firing events found in 7d window. Idempotent (push uses Loki's dedup-on-nanosecond-timestamp).<br>• **bootstrap.sh wiring** — must call the backfill once at install time and once after any upgrade that adds state-history config (so customers who install v1.x see history from the moment their stack comes up, not starting empty).<br><br>**Verified live on .220 (HTTP 200 on all 7 panels via `/api/ds/query`):**<br>• `Still Open` = 3<br>• `Firing Events — This Week` = 13<br>• `Top 5 Devices — 30 Days` query 0.56 s · `6 Months` 3.1 s · `12 Months` 6.4 s — acceptable for a dashboard opened consciously; not for 30 s auto-refresh wall. If 6 s ever becomes a problem, reduce `max_query_parallelism` per request or move the 365d query to a stats-cache loader.<br><br>**Caveats / known gaps:**<br>• **Inventory hostnames are blank** for every device on .220 right now (verified live — `discover.py` PR for reverse-DNS pending BACKLOG #41). Until that lands, panels show IPs in `labels_host`. Dashboard is correct as soon as hostnames populate; no code change needed.<br>• **Top offenders rank by device, not by rule.** Customer choice (memory: IT staff remember hostnames). If ranking by rule becomes useful, add a second set of three panels keyed on `labels_alertname`.<br>• **Time picker only affects the Recent Firing Events logs panel.** The `[7d] / [30d] / [180d] / [365d]` windows are baked into LogQL so the rolling counts stay stable across time-picker changes. Documented in the panel description strings.<br>• **No "MTBF / MTTR per device"** yet — would require computing `avg_over_time(up{host=...} == 0)` vs firing-event count. Captured as a follow-up, not in scope for v1.x.<br>• **No "alertmanager health" panel** — `grafana_alerting_state_history_writes_failed_total{backend="loki"}`. If Loki is down, this dashboard silently goes wrong. Worth adding in v1.x.x once we have at least one customer install to test the failure mode on.<br><br>**Out of scope for v1.x (park as separate items if surfaced):**<br>• Tying Alertmanager to ticketing flow<br>• MTBF / MTTR per device<br>• Severity breakdown (critical vs warning)<br>• Per-rule top 5 alongside per-host<br>• Silenced / inhibited count panels | Medium |
| 25 | Inventory `delete_device` tool + confirmation flow | **[RESOLVED — PR #10](https://github.com/rylanddufour/AIAMSBS/pull/10), merged 2026-06-27.** Added `delete_device(device_id, cascade_relationships=True)` to `inventory-stack/mcp/server.py` (returns `deleted_record` so callers can show user what was removed). Updated `profiles/it_admin/skills/non-destructive-operations.md` with mandatory "search → show → confirm → delete" flow. Smoke test 12/12 → 14/14 PASS. E2E verified on VM 220: IT_ADMIN and default profile both follow confirmation pattern (search, show row, ask "confirm?", wait for explicit yes, then delete). Hard-delete only — soft-delete / audit trail is a follow-up when a customer needs it. | — |
| 23 | AIAMSBS release + CI/CD pattern | Design and implement the full dev → test → release → customer-update pipeline so a new AIAMSBS version (bootstrap.sh changes, new skills, container image updates, inventory schema migrations) can be (a) tested end-to-end on a dev VM, (b) committed via PR, (c) tagged as a release, and (d) automatically delivered to installed customer devices **without clobbering customer state** (skills created by the agent, inventory data, dashboard customizations, API keys, locally-modified SOUL.md).<br><br>Must address:<br>• **Versioning scheme** — semver for AIAMSBS itself; how to version bundled skills, the inventory DB schema, and the Docker stack separately so we can update one without bumping others<br>• **Customer update channel** — opt-in `aiamsbs upgrade` script vs. scheduled cron that watches a GitHub release vs. pull-from-customer-initiated `git pull && bash bootstrap.sh`. Trade-off: automation vs. customer control vs. forward-compatibility for breaking changes<br>• **Pre-update safety** — backup customer-modified skills, inventory DB snapshot, current SOUL.md, dashboard JSON before applying upgrade; restore on failure<br>• **Schema migrations** — inventory DB schema version table + migration runner; forward-only with backout plan documented<br>• **Skill update policy** — bundled skills update overwrites customer edits (current `cp` behavior). Need: detect modified-since-install, prompt before overwrite, or skip-by-default with `--force-skill-update` flag<br>• **Test gate** — what passes before tag? E2E smoke (`smoke_test.sh`), inventory MCP integration test, dashboard creds still work, both profiles still respond. Run on snapshot VM before merge to main, not after<br>• **Roll-forward + roll-back** — release tags + GitHub Releases; rollback = `aiamsbs upgrade --to <previous-tag>`<br>• **Channel strategy** — `stable` (every release), `beta` (release candidates), `lts` (long-term support for small shops that don't want to chase upgrades)<br><br>**Related work in flight:**<br>• Phase-04 §4 already calls out the current dangerous clobber paths (`install_*_profile_soul`, `generate_dashboard_credentials`, `configure_hermes_api`)<br>• BACKLOG #22 already gates agent self-modification of skills — needs to be paired with a *customer-aware* upgrade path so bundled skill updates from us don't fight with agent's edits<br>• Upgrade discussion (2026-06-27 Telegram) — three options sketched: marker-file idempotency + opt-in force flag (recommended), separate `aiamsbs upgrade` subcommand, smart skill diff. Decision pending Ryland.<br><br>**Out of scope for #23** (park them as follow-ups if #23 surfaces them):<br>• Telemetry / usage analytics from customer installs<br>• Customer-facing update UI (web dashboard)<br>• Air-gapped delivery (offline install package) | High |

### Metrics Fix (Pre-req for dashboards)

| # | Item | Description | Complexity |
|---|------|-------------|-------------|
| A | ~~Fix container metrics~~ | **[RESOLVED — commit b5d0249](https://github.com/rylanddufour/AIAMSBS/commit/b5d0249).** Enabled Alloy cAdvisor with host mounts so `container_cpu_usage_seconds_total` and other `container_*` metrics flow to Prometheus. Slimmed stack (`38b7d28`) replaced node-exporter + cAdvisor with Alloy container; cAdvisor duplicate metrics fix (`0bc097f`) added `docker_only` and Prometheus relabel config. | — |
| B | ~~Add hostname label~~ | **[RESOLVED — same as #10, PR #29 (commit 41e4119)](https://github.com/rylanddufour/AIAMSBS/commit/41e4119), 2026-07-17.** Identical fix to #10 (this row is the pre-req duplicate listed under "Metrics Fix"; #10 is the main entry in Medium Priority). | — |
| 5 | Log retention config | **[RESOLVED — commit 68fe095](https://github.com/rylanddufour/AIAMSBS/commit/68fe095), 2026-07-15.** PR #18 (4e9d204, 2026-07-03) added the config but it was never actually running on a deployed instance — the `loki` service in `docker-compose.yml` had no `command:` override, so the container started with the image default `-config.file=/etc/loki/local-config.yaml` (image-bundled: no retention, no compactor, no query limits, analytics opt-out ignored) and our bind-mounted `config/loki.yml` was silently ignored. Discovered 2026-07-15 via `curl :3100/config` showing `retention_enabled: false, retention_period: 0s` despite the policy being on disk. **Now:** (1) `docker-compose.yml` adds `command: -config.file=/etc/loki/local-config.yml` to the `loki` service so it actually reads the project config; (2) `config/loki.yml` gains 3 loki-3.2.0 compat fixes — `max_streams_match_per_query` → `max_streams_matchers_per_query` (the field was renamed in 3.x; old name is a fatal config error), `allow_structured_metadata: false` added to `limits_config` (we still run schema v12/boltdb-shipper; opt out of the v13 requirement), `compactor.delete_request_store: filesystem` added (required when `retention_enabled: true`, otherwise the compactor refuses to start). **Verified live on .220:** `loki /config` returns `retention_enabled: true, retention_period: 90d, max_streams_matchers_per_query: 1000, max_entries_limit_per_query: 5000, reject_old_samples_max_age: 1w, reporting_enabled: false`; 4 `loki_compactor` series emitted (compactor running). Single global policy across all streams (aiamsbs_host, network_device, future hermes-*). The earlier "Loki 3.x runs the compactor in-process; no docker-compose change needed" claim in the PR #18 entry was wrong. | Low |
| 6 | Backup script | **[RESOLVED — commit 40b83de](https://github.com/rylanddufour/AIAMSBS/commit/40b83de), 2026-07-08.** Was: `scripts/backup-dashboards.sh` only exported Grafana dashboards via the API (~15 KB per archive). The customer could not recover the rest of the AIAMSBS stack from a backup, since the customer can never push changes to the aiamsbs repo (BACKLOG #6 problem statement). Now: `scripts/aiamsbs-backup.sh` (223 lines) captures everything needed for disaster recovery in a single `~/backups/aiamsbs-backup-<ts>.tar.gz` — Grafana dashboards (API export + provisioning source files), hermes state (one zip via `hermes backup`), inventory + KB SQLite DBs (python3 `src.backup()` inside the containers — `sqlite3` CLI is not in those images), and all 7 Grafana-stack yml configs (alloy, blackbox, loki, prometheus, promtail, datasources, dashboards.yml). A `MANIFEST.json` records per-section counts + sizes. Renamed the Hermes cron `AIAMSBS Dashboard Backup` → `AIAMSBS Backup` (id `aiamsbs-backup-it_admin` and `0 1 * * *` schedule unchanged; existing `jobs.json` entry migrated in-place so `last_status` history is preserved). Verified on VM 220: manual run + forced `hermes cron run` both produced a 46 MB tarball; next scheduled run 2026-07-09T01:00Z. Skill renamed `dashboard-backup.md` → `aiamsbs-backup.md` (142 lines: exit codes, env overrides, troubleshooting matrix, exclusion rationale). Excluded intentionally: Loki chunks + Prometheus TSDB (their own retention), hermes-agent code (PyPI-installable), Docker volumes (service-level). **Still out of scope for #6:** off-host backup target (rclone / restic / NAS — same-VM is not a real backup), encryption at rest (hermes zip contains API keys in plain text). | Low |
| 6a | Hermes WebUI scheduled jobs | Enable gateway in container so cron jobs work in WebUI | Medium |
| 33 | Hermes gateway as system service (VM-side) | **[RESOLVED — branch `wt/gateway-system-service-20260704`, 2026-07-04.]** Pre-fix: `bootstrap.sh` installed the dashboard as a system service but left the hermes-gateway daemon manual, so on reboot the `AIAMSBS Dashboard Backup` cron entry (registered in `~/.hermes/cron/jobs.json` by `install_dashboard_backup_hermes_cron`) was inert — `jobs.json` showed `state: scheduled` and `enabled: true` but no `last_run_at` ever advanced. Root cause: per `hermes_agent/cron/__init__.py`, *"Cron jobs are executed automatically by the gateway daemon"*. The gateway MUST be running for any `hermes cron` job to fire. Fix: new `install_hermes_gateway_service()` function in `bootstrap.sh` calls `sudo hermes gateway install --system --run-as-user <user>`, which writes `/etc/systemd/system/hermes-gateway.service` (multi-user.target boot, independent of user login), and explicitly `systemctl start`s + `is-active` verifies. Function is idempotent (skips when unit file already present) and degrades gracefully on non-systemd hosts (skips silently like the dashboard service). Wired into `main()` after `start_hermes_dashboard` and before `install_dashboard_backup_hermes_cron` so the gateway is up before the cron first registers. `profiles/it_admin/skills/dashboard-backup.md` updated with a "gateway is what ticks the cron" section + new troubleshooting row for the "scheduled but never fires" symptom. Related to #6a (WebUI container equivalent — different runtime, same root cause). | — |

### Testing

| # | Item | Description | Complexity |
|---|------|-------------|-------------|
| 11 | Test syslog with real network device | **[RESOLVED 2026-06-30]** Promtail syslog receiver on :514 confirmed end-to-end — firewall logs visible in the Grafana dashboard after E2E runs (verified by Ryland). Multi-OEM syslog path proven. | — |


### Low Priority

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 7 | TLS/HTTPS for all services | Enable automatic HTTPS via nginx+certbot | Medium |
| 8 | ~~Service dependency health~~ | **[WITHDRAWN 2026-07-21 — Ryland decision.]** Dependency-graph view never built. Per-service health (covered by #2 health check dashboard) is sufficient for the customer persona. Closing as not-needed; reopen if a future customer asks for cross-service dependency visualization. | — |
| 36 | Grafana dashboard UI-edit workflow | File-provisioned dashboards can't be edited in the UI (`allowUiUpdates: false` in `config/grafana/provisioning/dashboards/dashboards.yml`). The export-JSON-paste-to-agent-redeploy workflow is clunky.<br><br>**Attempted 2026-07-07 on VM 220 (Windows Exporter dashboard, panel layout):** flipped to `allowUiUpdates: true` + `updateIntervalSeconds: 0` → UI edits work → still no clean way to commit UI changes back to the file. Rolled back to avoid leaving provisioning unlocked.<br><br>**Need:** a seamless UI-edit → file round-trip. Options to evaluate:<br>• (a) Keep `allowUiUpdates: true` permanently with `updateIntervalSeconds: 0` (UI becomes the working copy; risk: file drifts if someone re-enables the watcher later)<br>• (b) Auto-serialize UI dashboard JSON back to the provisioning file on Save (Grafana plugin or post-save hook)<br>• (c) Non-provisioned duplicate for UI editing, then JSON-copy back to the provisioned file<br>• (d) A `kanban: t_...` workflow card that automates the round-trip end-to-end<br><br>Until this is solved, every dashboard tweak needs a manual file edit + provisioning reload. Hit again immediately after while iterating the Windows Exporter Identity row layout. | Medium |
| 9 | Metrics for Hermes itself | Monitor Hermes Agent with Prometheus | Medium |
| 26 | Blackbox HTTP/TCP probes for service health | **[RESOLVED — pending commit on wt/clean-install-e2e-20260627, 2026-06-28.** Added `prom/blackbox-exporter:latest` service in `docker-compose.yml` (network_mode: host, port 9115) with `config/blackbox.yml` defining three modules: `http_2xx` (readiness + MCP roots), `http_2xx_login` (accepts 2xx+3xx for Hermes Dashboard `/login` 302 redirect), and `tcp_connect` (Promtail syslog :514). Prometheus scrape jobs (`blackbox`, `blackbox_login`, `blackbox_tcp`, `blackbox_exporter` self-metrics) added to `config/prometheus.yml`. Probes hit `localhost` from inside the container so the host-side services are reachable. Verified E2E on VM 220 — see commit for live `probe_success` values per endpoint. | Low |
| 34 | Tidy up the repo (kanban: t_2d5db13a) | General repo hygiene pass — find and remove dead/orphaned/stale content that has accumulated across the many iteration commits (BACKLOG.md Traefik scrub, dashboard re-orgs, profile splits, bootstrap.sh force-pushes). Scope is intentionally broad; Ryland will narrow when picked up. Known candidates to investigate (not exhaustive — start with these, then sweep):<br><br>• **Stale dashboard duplicates** — `dashboards/` at repo root has frozen copies of files that are also (correctly) in `config/grafana/provisioning/dashboards/`. The repo-root copies drift and confuse subagents (2026-07-04 incident: subagent read only the repo-root copy, escalated a false CRITICAL). Decide: delete repo-root `dashboards/` and rely on provisioning, OR move it under a clearly-labeled `dashboards.archive/` dir.<br>• **Dead code** — files in the repo that nothing references (`grep -rln <file> bootstrap.sh GOAL.md README.md docker-compose.yml .env.example` = 0 matches = dead). Past examples: original `docker-compose.mcp.yml` (5 MCP servers defined, none deployed), `inventory-discovery/` skill stub at repo root (superseded by the installed skill).<br>• **Stale references in docs** — README.md, GOAL.md, BACKLOG.md, INSTALL.md may still mention removed services (Traefik scrub was a focused pass; full sweep may find more), removed profiles (#16-#19 retired, but mentions elsewhere?), superseded paths.<br>• **Bootstrap.sh cruft** — function names, prompts, and `cat <<EOF` blocks referencing old layouts. With ~30+ commits and several force-pushes, drift is expected.<br>• **Local dev paths in shipped content** — anything mentioning `/home/openclaw/...` (dev box) instead of `$INSTALL_BASE_DIR/...` (customer VM). Customer VMs don't have `/home/openclaw`.<br>• **Outdated OneDrive mirrors** — the `obsidian_vaults/agent vault/...` mirrors are point-in-time; may not match current repo state.<br><br>**Process rules (per Ryland's iteration pattern):** minimal-change-first, separate commits per cleanup target, no bundling. Each removal = its own commit with a clear message so rollback is granular. Worker MUST NOT reformat/rewrite files as part of the cleanup — touch only the lines that match the target. If something looks like it should be restructured, flag it in the task report and stop. **Worker MUST NOT call `kanban_create` for follow-up work** — any side findings go in a comment on the task card.<br><br>**Acceptance criteria:** (1) per-target commits on a feature branch, (2) `git log` shows one commit per cleanup target, (3) `git diff --stat` against main shows only the targeted content removed (no incidental reformatting), (4) E2E smoke (`smoke_test.sh`) still passes — no cleanup removed something the stack actually depends on. | Medium |

---

## Inventory & Multi-Agent (New Capability Track)

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 13 | Coordinator profile (deferred) | Build dedicated coordinator profile that routes alerts via inventory MCP to specialist profiles. **May become the default profile** (under review 2026-06-26 — see decision D1). Depends on inventory MCP (#14). | High |
| 14 | ~~Inventory MCP stack~~ | **[RESOLVED — commit 38050fb](https://github.com/rylanddufour/AIAMSBS/commit/38050fb), 2026-06-27 (E2E verification).** `inventory-stack/` subdir with own `docker-compose.yml`; FastMCP server exposing 7+ tools (`get_device`, `create_device`, `update_device`, `search_devices`, `list_devices`, `delete_device`, etc.); nmap-discovery container (`scan_type=ping|deep`); registered in customer's default + IT_ADMIN profiles. Wired into `bootstrap.sh` deploy_inventory_stack + register_inventory_mcp. | — |
| 15 | Ansible container (scope TBD) | Ansible container for bulk operations against managed devices. Scope TBD 2026-06-26 — may be needed by IT_ADMIN (#20) for fleet-wide changes; depends on whether IT_ADMIN ships with automation tools or relies on bash-tool-only. | Medium |
| ~~16~~ | ~~linux_admin Profile~~ | **RETIRED 2026-06-26.** Superseded by IT_ADMIN (#20). The 3 skills shipped in PR #1 were reverted in PR #2. | — |
| ~~17~~ | ~~network_admin Profile~~ | **RETIRED 2026-06-26.** Absorbed into IT_ADMIN (#20) as `skills/network-oem-*` modular skills. | — |
| ~~18~~ | ~~windows_admin Profile~~ | **RETIRED 2026-06-26.** Absorbed into IT_ADMIN (#20) as `skills/windows-server.md` + `skills/active-directory.md`. | — |
| ~~19~~ | ~~vsphere_admin Profile~~ | **RETIRED 2026-06-26.** Absorbed into IT_ADMIN (#20) as `skills/vsphere-admin.md`. | — |
| 20 | ~~IT_ADMIN Profile~~ | **[RESOLVED — commit 983a2ba](https://github.com/rylanddufour/AIAMSBS/commit/983a2ba), 2026-07-08.** `profiles/it_admin/SOUL.md` + 23 skill files (sourced from OneDrive: `obsidian_vaults/agent vault/AIAMSBS_Potential_Agent_Soul_skills/...`). Installed by `bootstrap.sh install_it_admin_profile_soul()` (line 520). Inherits provider/model/base_url from default profile. Replaces the planned 4 specialist profiles (#16-19, RETIRED 2026-06-26). Sibling to `default`, not a child. | — |
| 39 | ~~Scheduled inventory discovery + blackbox-from-inventory (Phase 2 Network Discovery)~~ | **[RESOLVED — commit 2ced874](https://github.com/rylanddufour/AIAMSBS/commit/2ced874), 2026-07-11.** All 7 sub-items shipped on `feature/network-discovery-phase2-20260710` → merged to main. `discover.py` upsert fix (`3a23f78`); `list_devices` MCP tool; `scan_type=deep` for nmap-discovery; `--auto-detect-subnet` flag; `regenerate_blackbox.py` + Prom `file_sd_configs` + `icmp` blackbox module; Hermes cron `inventory-discovery` (`0 2 * * *`); `kickoff_inventory_discovery()` in bootstrap.sh. Bonus: Inventory — Device Status Grafana dashboard (`da5853d`), updated architecture diagram (`1da4e11`), process doc `docs/inventory-discovery-and-blackbox.md`. Post-merge fixes: `9ae7c82` (NET_RAW for ICMP), `4d8b37e` / `f5068a8` / `dedb999` / `fe3e5e4` / `3a54076` (FastMCP response parsing), `ea7c035` (`--max-rate 10` to catch slow-ARP hosts). Deferred sub-items tracked as #40 (multi-target nmap), #41 (DNS reverse lookup), #42 (polystat panel render). **Note:** `config/discovery.yml` config-file override mentioned in this entry was never implemented — cron uses hardcoded defaults in the prompt instead (no behavior change for v1; no customer-facing impact). | — |
| 40 | nmap-discovery multi-target support (deferred from BACKLOG #39) | `inventory-stack/nmap-discovery/server.py` invokes nmap as `subprocess.run(["nmap", "-sn", "-PR", "-oX", "-", target])` — a SINGLE argv element for the target. nmap can't parse space-separated lists when the space survives into argv. So `discover.py --auto-detect-subnet` can't force-include the default gateway as a separate target. **Today** the gateway is in the same /24 as the host (so ARP sweep finds it). **When needed:** if a customer's gateway is on a different subnet than the host (multi-NIC, DMZ, etc.), the gateway would be missed. **Fix:** modify nmap-discovery to accept either (a) a repeated `target` query param, or (b) a new `targets` field that's a JSON array, then split into argv elements in server.py. Sub-item 4 was shipped without this (#39 deferred it). Picked up after #39 ships. | Low |
| 41 | DNS reverse lookup on discovered hosts (deferred from BACKLOG #39) | The v2 brief calls for DNS reverse-lookups via the host's configured DNS resolvers so discovered devices get a hostname/FQDN, not just an IP. The current `discover.py` (sub-item 1) populates the `hostname` field as `""` because no reverse-lookup step exists — verified on the .220 inventory: 55 devices, all `hostname=""`. **Fix:** add a `reverse_dns_lookup(ip)` helper in `discover.py` that calls `socket.gethostbyaddr(ip)` (uses the AIAMSBS host's configured `/etc/resolv.conf` resolvers automatically), captures the first element of the returned tuple (FQDN or short hostname), and falls back to the IP on any exception (timeout, NXDOMAIN, network unreachable). Wire into `device_payload()` so the `hostname` field is populated. The `socket.gethostbyaddr` default timeout is whatever the system resolver uses (typically 5s × 3 retries = 15s max per lookup). For a /24 scan with an unreachable DNS, worst case is 54 × 15s = 810s — exceeds the cron timeout of 600s. Acceptable v1 risk; future hardening: parallelize with `concurrent.futures.ThreadPoolExecutor` and per-call timeouts. E2E: re-run discover.py against a /24; the inventory's `hostname` column should be populated for hosts that have a PTR record. | Low |
| 42 | ~~Inventory — Device Status dashboard: Polystat render broken (deferred from BACKLOG #39)~~ | **[RESOLVED — workaround, commit a145a1b](https://github.com/rylanddufour/AIAMSBS/commit/a145a1b), 2026-07-10.** Polystat plugin stays installed but is no longer used by the dashboard. Panel #4 reverted to plain `type: "stat"` with `repeat: "instance"` + `maxPerRow: 12` + `gridPos.h: 2` — 55 device tiles render correctly with FQDN text and red/green threshold color. Grafana 13 stat panel has no `shape` property; hexagons were silently dropped. **Closing as "won't fix" (not "fixed"); the original Polystat v2 approach was abandoned, not completed.** | — |
| 43 | ~~Auto-discover linux_exporter / windows_exporter targets~~ | **[RESOLVED — superseded by #44].** Original premise (auto-detect `:9100` and `:9182` via nmap deep scan + write targets to `prometheus.yml`) is dead. Alloy migration (#44) replaced `node_exporter` / `windows_exporter` jobs with Alloy's native `integrations/unix` and `integrations/windows` scrape jobs (see `prometheus.yml:151-159`). Different mechanism achieves the same customer goal (Phase 1 alert rules have data to evaluate against) without needing a separate auto-discovery flow. | — |

## Knowledge Base (New Track)

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 30 | Knowledge Base feature (overall) | Design captured 2026-06-30 in OneDrive: `obsidian_vaults/agent vault/AIAMSBS_Docs_Diagrams/kb_workflow.md`. MVP scope defined: SQLite + sqlite-vec, `kb-mcp` on `:8002` exposing `kb_search` / `kb_add` / `kb_update` / `kb_list` / `kb_delete`, single vector index over (1) bundled skills, (2) customer-supplied docs, (3) runtime KB. Standalone HTML viewer served by `kb-mcp` at `:8002/ui/`. Doc ingestion = paste text + file upload (no URL fetch in MVP). Auto-capture = Mode C with trust ladder (Levels 0-3, customer climbs over time). Write access = both agent and customer, customer can always edit/delete agent entries. Customer-supplies OEM-specific docs (no bundled curated OEM knowledge — that was rejected as a research tarpit). Agent attempts task first using all five sources (skills, training, runtime KB, customer docs, tools); asks the human only on graduated uncertainty. | High |
| 31 | KB self-correcting loop | Customer edit/correction of a wrong agent-generated KB entry must propagate to (a) agent's future behavior — corrected entry replaces the wrong one, agent prefers it on the next attempt, (b) trust ladder stats — corrections count against the agent's "clean approval streak" and adjust level-up suggestions. This is the loop that turns "the agent was wrong" into "the agent doesn't make the same mistake twice" — the whole point of Mode C. Design not started; depends on #30. The customer correction surface is part of the standalone HTML viewer; the propagation logic lives in `kb-mcp` and the runtime KB write path. **Backlogged 2026-06-30 per Telegram thread — Ryland flagged the loop as the missing piece after the design was otherwise locked in.** | Medium |
| 47 | KB ingestion from SMB/NFS shares (sub-item of #30) | Extension to #30 doc ingestion. When the customer prompts the agent to ingest KB content from a Windows file share (CIFS) or NFS export, the AIAMSBS host needs the client tooling installed so the agent can mount the share and read files. **Mechanism:** `deploy_kb_stack()` in `bootstrap.sh` now installs `cifs-utils` (provides `mount.cifs`), `nfs-common` (provides `mount.nfs`), and `smbclient` (CLI browser for share discovery + quick reads) — only when the KB stack is being deployed, dpkg-checked, idempotent. **Operational flow (not yet built — UI surface only):** customer asks the agent to ingest a share → agent uses `mount.cifs`/`mount.nfs` (with creds the customer supplies) to mount → reads files → calls `kb_add` per file. **Scope of THIS row:** packages + BACKLOG tracking only. The actual mount-orchestration logic (a new `kb_ingest_share` tool, a UI surface for share configuration, credential storage) is **NOT in scope** here — the row unblocks customers who want to point the agent at a share today via the terminal; the productized path is a follow-up item. **Backlogged 2026-07-22 per Telegram thread.** Repo change only; no live install on .220 (per AIAMSBS .220 rule — packages land on next bootstrap). | Low |
| 48 | Deploy vaultwarden as sibling docker-compose service (prereq for #50) | Sibling docker-compose service alongside `kb-mcp`, `grafana`, `prometheus`, `loki`. Bind on `127.0.0.1:8003` (web vault stays local; reverse proxy + TLS for customer-facing UI). Persistent volume for `/data`. **Hardening (set on first run, documented in bootstrap):** `SIGNUPS_ALLOWED=false`, admin token, optional fail2ban. **Why:** `kb_ingest_share` (#50) needs a vault backend; vaultwarden is the customer-controlled credential store. Ship it before any MCP that talks to it. **Out of scope (BACKLOG rows):** bitwarden/mcp-server wiring (#49), `kb_ingest_share` MCP tool (#50), first-user setup wizard, doc+diagram cards (#51). **Backlogged 2026-07-22 per Telegram thread.** | Low |
| 49 | Wire `bitwarden/mcp-server` against vaultwarden (prereq for #50) | Deploy `bitwarden/mcp-server` (https://github.com/bitwarden/mcp-server, 204★, official Bitwarden MCP) as a **host-local** MCP server — NOT in a container, per its own README warning ("designed exclusively for local use and must never be hosted publicly"). Configure with `BW_API_BASE_URL=http://127.0.0.1:8003` and `BW_IDENTITY_URL=http://127.0.0.1:8003` (pointing at the vaultwarden instance from #48). Auth: vaultwarden admin token (or client_credentials API key) stored in `~/.hermes/.env` on the host, sourced by the MCP server's launch env. Register in `default` and `it_admin` Hermes profiles (follows the existing `inventory-mcp` / `kb-mcp` / `grafana-mcp` registration pattern in `bootstrap.sh`). **Tool surface used:** `get_item`, `list_items`, `create_item`, `edit_item`, `delete_item` — exactly what `kb_ingest_share` needs. **Verified vaultwarden compat (2026-07-22):** the MCP server depends on `@bitwarden/cli` (`bw`); `bw` works against any Bitwarden API endpoint via `BW_API_BASE_URL`/`BW_IDENTITY_URL`; vaultwarden implements both endpoints. **Out of scope (BACKLOG rows):** `kb_ingest_share` MCP tool (#50, the consumer), `env_loader.py` patch to make Hermes's own secrets API support vaultwarden (deliberately skipped — Hermes `secrets.bitwarden` is BSM-only and not user-pluggable per the source; we don't need it). **Backlogged 2026-07-22 per Telegram thread (post research-profile correction).** | Low |
| 50 | `kb_ingest_share` — MCP tool for SMB/NFS share ingestion (the headline) | Delivers the productized path for BACKLOG #47. **New MCP tools on `kb-mcp`:** `kb_add_share`, `kb_list_shares`, `kb_delete_share`, `kb_ingest_share`, `kb_ingest_status`. **Host-side worker:** new systemd unit `aiamsbs-kb-share-mounter.service` polls `kb_share_ingestion_jobs` table, resolves the vault item via `bitwarden/mcp-server` (#49), mounts with `mount.cifs`/`mount.nfs`, walks files, calls `kb_add` per file, unmounts. **Schema additions:** `kb_share_credentials` (vault-item-ref only — actual password never enters SQLite), `kb_shares` (registered shares), `kb_ingestion_jobs` (job tracking). **Idempotency:** each ingested entry tagged `bulk_ingest=true`, `share:<id>`, `path:<relpath>`, `sha256:<hash>`; re-ingest skips files with matching sha256+share+path. **File filter:** `.txt .md .rst .log .csv .json .yaml .ini .conf` by default; configurable glob per call; PDF/DOCX extraction = separate follow-up. **Trust ladder:** all share-ingested entries start `status=pending`, `bulk_ingest=true`; K3 review queue will offer bulk-approve (K3 ships the UI). **Out of scope (separate follow-ups):** PDF/DOCX text extraction, auto-mount-on-boot, scheduled re-sync, watch-mode, share-config UI, credential storage in `~/.hermes/.env` for the host worker (handled by the `BW_*` env vars on the bitwarden/mcp-server launch). **Blocked on:** #48 (vaultwarden), #49 (bitwarden/mcp-server), #47 (packages). **Backlogged 2026-07-22 per Telegram thread.** | High |
| 51 | KB ingestion docs + diagram cards (deferred until #48-50 work end-to-end) | Two deliverables, both gated on #48-50 working on .220: **(a) Documentation card:** update `kb_workflow.md` (extend decision #6 with the full vaultwarden story, add "Credential storage" subsection, replace the BACKLOG #47 placeholder for the operational flow with the actual productized path) and `kb-stack-README.md` (add "Vaultwarden integration" section, list `bitwarden/mcp-server` in the dependencies table). **(b) Diagram card:** new architecture diagram showing data flow `customer → agent → kb_ingest_share → kb_share_ingestion_jobs → host mounter service → bitwarden/mcp-server → vaultwarden → SMB/NFS share → kb_add`, Excalidraw format to match existing `diagrams/` convention, mirrored to OneDrive (`obsidian_vaults/agent vault/AIAMSBS_Docs_Diagrams/`). **Out of scope (same as the feature rows):** same follow-ups as #50. **Backlogged 2026-07-22 per Telegram thread.** | Low |

## Customer Onboarding (New Capability Track)

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 52 | Customer post-install success plan (crawl → walk → run) | Once bootstrap.sh finishes and the stack is live, what does the customer **do** with AIAMSBS? Today the install ends at "[SUCCESS] Stack reachable at <http://IP:3000>" and the customer is on their own. This row defines the customer-facing success path from "day 0 inventory" through "autonomous troubleshooting life cycle" as a staged **crawl → walk → run** plan, each phase with concrete prompts the customer (or their ops folks) actually run, plus the configuration that must exist before those prompts do anything useful.<br><br>**Deliverable:** `docs/post-install-success-plan.md` (mirrored to OneDrive: `obsidian_vaults/agent vault/AIAMSBS_Docs_Diagrams/`, NOT committed to repo per the GitHub strategy rule). The doc is the script a customer follows end-to-end; BACKLOG #53 is the matching use-case catalogue referenced from the doc.<br><br>**Phase A — CRAWL (week 1, ~30 min total customer time):** establish visibility, no automation yet.<br>• **A1. Inventory the network.** Prompt: *"Inventory the 192.168.0.0/24 subnet"* (or whatever the customer's subnet is). Prereq: discover.py + nmap-discovery container running (BACKLOG #14, #39). Outcome: device list in inventory-mcp, dropdown in Inventory — Device Status dashboard populates.<br>• **A2. Manually add what discovery missed.** Prompt: *"Add server db-prod-01 at 192.168.0.50, OS=Windows Server 2022, role=SQL"* (and similar for switches/firewalls the customer knows by name). Seeds the canonical-name → IP mapping the agent will use for every later prompt.<br>• **A3. Forward first network device's syslog.** Configure Promtail :514 receiver (BACKLOG #35) → point one firewall / switch at it → verify "Network Device Logs" dashboard panel shows entries. Config: 1 outbound syslog line on the device, no agent install.<br>• **A4. Open the health check dashboard.** Confirm the customer sees: services up, 1 host in node-exporter dropdown, ≥1 entry in Network Device Logs. Success = "I can see my stuff."<br><br>**Phase B — WALK (weeks 2–4, ~2 hr total):** extend coverage, start using the agent, no autonomous actions yet.<br>• **B1. Forward syslog from every network device.** Same as A3 but across the fleet. New devices are auto-detected via Promtail's `source=network_device` label.<br>• **B2. Install Alloy on managed servers.** Either manually (BACKLOG #44 / `docs/multi-host-monitoring.md`) or via customer's existing config mgmt. After this, every `host` dropdown across the dashboards goes from 1 → N.<br>• **B3. Seed the KB with company context.** Prompts: *"Add to KB: our firewall vendor is Fortinet, support contract is with company X, account # is Y"*, *"Add: backup window for VMs is Sunday 02:00 UTC"*, *"Add: on-call rotation lives at <url>"*. Prereq: KB stack (BACKLOG #30) deployed; entries start at trust K0 / `status=pending`.<br>• **B4. Validate alert rules.** Confirm the 3 customer-server rules (BACKLOG #3) fire on a synthetic down event, notifications reach the customer's chat (Telegram/email — decide during install), and the customer knows how to ack a firing alert.<br>• **B5. Practice read-only questions.** Prompt catalogue the customer can run today: *"What's down right now?"*, *"Why did .211 alert?"*, *"Show auth failures on .211 in the last 24h"*, *"Compare CPU on .211 vs .212 over last 7d"*. All Phase B prompts are **read-only** (no changes) — builds trust in the agent before any mutation prompts are accepted.<br><br>**Phase C — RUN (month 2+, ongoing):** autonomous life cycle, with trust ladder.<br>• **C1. Alert-driven investigation.** When an alert fires → notification links to an automatically-prepared investigation packet (logs around the firing time, recent config changes, KB hits with similar symptoms). Mechanism: Hermes cron watches alert state (BACKLOG #46), on transition to `Alerting` for a customer-server rule → prompts `it_admin` profile to gather context → renders a Markdown report to Loki as `from=alert-investigation`. The cron prompt must be authored as part of this row so the customer's first firing alert produces a useful packet, not a stub.<br>• **C2. Knowledge capture mode.** Every resolved incident gets a KB entry (Mode C, BACKLOG #30): agent proposes wording → customer approves → trust ladder bumps. Trust ladder UI shipped as part of #51 / standalone HTML viewer (already in design). Target: after 3 months, 80% of "second time we saw X" questions answer from KB without re-asking.<br>• **C3. Weekly capacity / inventory report.** Scheduled cron: every Monday 08:00 local, generate a Markdown summary of (a) devices currently down, (b) top CPU/disk growth in last 7d, (c) new devices detected, (d) alerts fired last week, (e) KB entries added. Delivered to the customer's chat of choice. Single prompt template + Hermes cron + a small renderer; lives in `inventory-stack/` or `profiles/it_admin/skills/`.<br>• **C4. Self-improving loop.** Customer edit/correction of a KB entry triggers the propagation loop (BACKLOG #31): future invocations prefer the corrected entry; trust ladder reflects the correction. Without this, Mode C capture is just append-only log spam.<br>• **C5. Autonomous mutation prompts — gated.** Prompts like *"Reboot .219 — confirm first"*, *"Quarantine $HOST by removing from AD"*, *"Open support ticket with $VENDOR for $DEVICE"* are **gated** behind: (a) customer has reached trust K3, (b) confirmation pattern from BACKLOG #25 (`search → show → confirm → act`), (c) KB check for "has this been done before on this customer?". Phase C5 is gated — gating mechanism is part of the deliverable, not deferred.<br><br>**Out of scope (separate rows):** vCenter/SNMP expansion (#38, #37), Phase 2 multi-target nmap (#40), reverse DNS (#41), autonomous remediation beyond acknowledgment (#44 covers host metrics, not remediation), coordinated multi-agent (#13), KB trust-ladder UI (#51).<br><br>**Mechanism / delivery:**<br>• **Doc** — `docs/post-install-success-plan.md` (OneDrive-mirrored, see doc/diagram storage rule). Each Phase A/B/C prompt has: prerequisite, exact prompt text, expected response shape, "if it didn't work" troubleshooting link.<br>• **Bootstrap integration** — `bootstrap.sh` `print_post_install_plan()` runs at end of `main()`, prints a one-page TL;DR with the customer's first prompt ("Inventory <their-subnet>"), link to Grafana, link to the success-plan doc on OneDrive. No behavior change — purely an "and here's your next step" footer.<br>• **Cron stubs** — Phase C1 + C3 require two new Hermes cron entries registered by bootstrap (`install_success_plan_crons()`): `alert-investigation` (event-driven via the alert-events dashboard polling hook from #46) and `weekly-report` (Mon 08:00 local). Both register `ready` but disabled by default; customer opts in once they reach Phase C.<br>• **Profile scope** — prompts work for both `default` (read-only) and `it_admin` (mutations). The "C5 autonomous" prompts are explicitly `it_admin`-only.<br><br>**Acceptance criteria:**<br>• Doc draft lives at `~/AIAMSBS/docs/post-install-success-plan.md` AND mirrored to OneDrive (mirror confirmed via `rclone ls` listing)<br>• `bootstrap.sh` adds `print_post_install_plan()` footer; verified on VM 103 (or .220 if 103 is busy) — last 30 lines of post-install output reference the customer's actual subnet (read from `$AIAMSBS_HOST_SUBNET` if collected, else ask)<br>• Two new Hermes cron stubs (`alert-investigation`, `weekly-report`) appear in `jobs.json` post-install with `enabled: false`, so they don't run unless the customer opts in<br>• A 1-page printable quick-start (Markdown render → PDF, optional follow-up) referenced from the doc<br>• Each Phase C prompt has a smoke-tested path: agent can run it on .220 today against existing inventory + KB + alerts and produce a meaningful response (no new feature work — just verifiable that the doc's prompts work)<br><br>**Related:** #30 (KB — Phase C2/C4 depend on it), #35 (multi-host observability — Phase B2), #39 (discovery — Phase A1), #44 (Alloy — Phase B2), #3 (alert rules — Phase B4/C1), #46 (alert events — Phase C1 cron mechanism), #51 (KB docs+diagram — co-built), #53 (use cases — referenced from Phase B5).<br><br>**Backlogged 2026-07-29 per Telegram thread.** Kanban: `t_0138dac2` (created same turn as this row, unassigned, ready). | High |
| 53 | 20 AIAMSBS use-case catalogue (outcome + path per entry) | **CORRECTION 2026-07-29** — earlier draft framed this row as "20 prompts". Ryland clarified the same day: each entry is an **outcome** the platform achieves + a **path** to get there (multi-step, may involve configuration only — no single "prompt" required). Example from Ryland: outcome = "alert fires → support case auto-opens in ticketing system with investigation packet attached"; path = configure alertmanager webhook → configure ticketing integration → verify E2E. Re-cast below to match.<br><br>**Deliverable:** `docs/use-cases.md` (mirrored to OneDrive: `obsidian_vaults/agent vault/AIAMSBS_Docs_Diagrams/`, NOT in repo per the GitHub strategy rule). 20 entries across 5 categories.<br><br>**Each entry structure (6 fields, consistent across all 20):**<br>• **Outcome** — what success looks like (one sentence, customer-facing).<br>• **Path** — numbered steps the customer (or operator) follows to achieve it. May be pure configuration, may include optional agent prompts along the way. Not a single prompt.<br>• **Prerequisite** — what must already be in place (links to BACKLOG rows or setup steps).<br>• **Time to achieve** — realistic estimate for a small-shop operator (1-10 staff).<br>• **Acceptance signal** — how the customer verifies "this works" (concrete, observable).<br>• **Related** — cross-refs to other use cases (by entry number) or BACKLOG rows.<br><br>**The 20 use cases (5 categories, ordered by typical customer value):**<br><br>**A. Visibility outcomes (4):** what the customer can *see*<br>1. **Complete network inventory exists in AIAMSBS, refreshed nightly** — outcome: every device on the subnet is in inventory-mcp, new devices flagged on next scan. Path: enable inventory discovery cron (BACKLOG #14, #39) → run initial scan → manually add missed devices (BACKLOG #14 `create_device`) → review weekly diffs. Prereq: bootstrap done, agent running. Time: ~15 min. Acceptance: `list_devices` returns N devices where N matches expected subnet size ± known unknowns.<br>2. **Customer sees device up/down status in real time** — outcome: Inventory — Device Status dashboard shows live tiles, up = green/down = red per device (color > text per Ryland's convention). Path: ensure Alloy on managed servers (BACKLOG #44) OR syslog from network devices (BACKLOG #35) → confirm `up{job="integrations/unix"}` series exists per host → confirm `host` dropdown populates with N entries. Time: ~10 min once telemetry source confirmed. Acceptance: dashboard renders N tiles, colors update within 30s of a synthetic host down event.<br>3. **Customer sees live syslog from every network device** — outcome: Network Device Logs panel populates with one entry per device. Path: configure one firewall/switch to forward syslog to AIAMSBS host `:514` (BACKLOG #35) → verify Loki shows entries with `source=network_device` label → repeat for each network device. Time: ~5 min per device. Acceptance: device dropdown in Network Device Logs dashboard has N entries, "Recent Logs" panel shows entries newer than 5 min ago.<br>4. **Customer sees per-host metrics for every managed server** — outcome: per-host dropdown in dashboards has N entries (one per server), node_exporter-style panels populate. Path: install Alloy on each managed server (BACKLOG #44 / `docs/multi-host-monitoring.md`) → verify `up{job="integrations/unix"}` shows N hosts → verify `$host` dropdown works in per-host dashboard. Time: ~20 min per server (including verification). Acceptance: per-host dashboard renders CPU/memory/disk/network panels with non-zero data.<br><br>**B. Alert-driven outcomes (5):** what happens *when alerts fire*<br>5. **Alert fires → support case auto-opens in customer's ticketing system with investigation packet attached** (Ryland's example). Path: configure alertmanager webhook in `config/alertmanager.yml` → configure ticketing integration (Jira/ServiceNow/Zendesk API — customer picks) → verify E2E with synthetic alert (BACKLOG #3 customer-server rule) → attach `from=alert-investigation` Loki packet as case comment. Prereq: ticketing system API credentials in `~/.hermes/secrets/`. Time: ~2 hr initial setup, fully automated after. Acceptance: synthetic alert fires within 60s, ticket appears in customer's ticketing system with the packet attached or linked.<br>6. **Alert fires → on-call gets paged with relevant context** — outcome: notification includes what alert, what device, what to look at first (not just "something is down"). Path: configure notification channels (Telegram / email / PagerDuty / Slack) → populate alert template with device context (hostname, IP, role from inventory-mcp) + KB hits → verify on synthetic alert. Prereq: BACKLOG #3 deployed, channel credentials configured. Time: ~30 min. Acceptance: synthetic alert triggers a notification containing "device .211" + a 1-line summary, not just the rule name.<br>7. **Alert fires → investigation packet ready before engineer asks** — outcome: Loki `from=alert-investigation` stream has a Markdown report for each firing alert, with timeline + logs + KB matches. Path: enable `alert-investigation` Hermes cron (BACKLOG #52 C1, depends on BACKLOG #46 plumbing) → verify cron ticks on alert transition (synthetic alert within 60s should produce a packet). Time: ~15 min after the cron is enabled. Acceptance: synthetic alert fires → within 2 min, a packet exists in Loki with the device's last-hour logs + relevant KB entries.<br>8. **Alert fires → agent proposes remediation from KB** — outcome: response includes "based on KB entry X, the fix is ...". Path: KB seeded with past incidents (BACKLOG #30 Mode C) → BACKLOG #31 self-improving loop active → verify agent pulls matching KB entry by symptom (e.g., "disk full" matches KB entry "How we clean up disk-full on Windows Server"). Time: ongoing. Acceptance: synthetic alert fires → agent response names a specific KB entry + the procedure from it.<br>9. **Alert volume tuned to chronic offenders** — outcome: customer can see "which device fired alerts N times in 30/180/365 days" (Alert Events dashboard, BACKLOG #46). Path: BACKLOG #46 deployed → review weekly → adjust alert thresholds for chronic offenders (raise the bar or add exclusion). Time: ~5 min/wk ongoing. Acceptance: chronic-offender bar gauge shows top-5 devices with non-zero counts, customer can click through to see their firing history.<br><br>**C. Investigation outcomes (4):** ad-hoc questions answered<br>10. **"Why did X alert?" → engineer gets a timeline + suspected cause** — outcome: agent response includes timestamped sequence (config change → resource exhaustion → alert fire) + KB matches. Path: BACKLOG #46 state-history for alert context + Loki logs around firing time (BACKLOG #5 retention 90d) + KB lookup (BACKLOG #30). Time: ~30 sec per query. Acceptance: query produces a Markdown response with at least 3 timeline events + 1+ KB match.<br>11. **Log search across all hosts** — outcome: agent can find SSH failures / OOM / disk-full events across the estate in seconds. Path: Alloy on managed hosts → Loki retention 90d (BACKLOG #5) → agent uses Loki MCP (or Loki datasource via Grafana MCP). Time: ongoing. Acceptance: query "show SSH failures on .211 in last 24h" returns ≥1 line for a host with auth failures, or zero with explanation if none.<br>12. **Capacity trend comparison across hosts** — outcome: side-by-side CPU/memory/disk chart for chosen hosts, time-series visible. Path: Alloy on multiple hosts → Prometheus retention (default 15d) → per-host dashboard panels (BACKLOG #35). Time: ~10 min setup. Acceptance: per-host dashboard renders the chosen hosts' CPU trend lines over 7d, customer can pick a different `$host` and see it update.<br>13. **Anomaly detection — "which device logged the most errors?"** — outcome: top-N device list with click-through to per-host logs. Path: Loki aggregation query (topk by error count over 24h) → scheduled weekly report (BACKLOG #52 C3) → dashboard panel. Time: ~20 min. Acceptance: query returns ranked list of devices by error count, click on a device opens its log view.<br><br>**D. Capacity / planning outcomes (3):** forward-looking<br>14. **Disk fill prediction** — outcome: customer gets alert before disk fills (e.g., 7 days ahead). Path: BACKLOG #3 `cust-srv-disk-fill-soon` alert (`predict_linear`) → verify with synthetic disk-fill scenario (write to a test partition). Time: ~15 min. Acceptance: synthetic disk-fill scenario produces a firing alert with a "fills in ~Xd" prediction in the description.<br>15. **Weekly capacity + incident report delivered to customer's chat** — outcome: every Monday 08:00 local, a Markdown summary hits Telegram/email with (a) devices currently down, (b) top CPU/disk growth in last 7d, (c) new devices detected, (d) alerts fired last week, (e) KB entries added. Path: enable `weekly-report` Hermes cron (BACKLOG #52 C3) → verify delivery + format. Time: ~10 min. Acceptance: Monday 08:00 the customer receives a Telegram/email with all 5 sections populated (not empty placeholders).<br>16. **New device on network → customer gets notified** — outcome: Telegram/Slack ping when scheduled discovery finds a new IP. Path: scheduled discovery (BACKLOG #39) + alert rule on count of new devices → verify. Time: ~20 min. Acceptance: synthetic "new device" event (e.g., spin up a new VM in the subnet) produces a notification within 30 min of next scheduled discovery run.<br><br>**E. Lifecycle outcomes (4):** repeat operations<br>17. **New server onboarded end-to-end in <30 min** — outcome: server appears in inventory, dashboards, alerts, KB. Path: KB add (vendor info, support contract) → inventory add (BACKLOG #14 `create_device`) → Alloy install on server (BACKLOG #44) → blackbox probe configured → dashboard `$host` dropdown updated → alert rules scoped to include it. Time: ~30 min for an experienced operator. Acceptance: server shows up in Inventory — Device Status dashboard, has metrics in per-host dashboard, KB has a row with vendor info.<br>18. **Device offboarded with full audit trail** — outcome: device gone from inventory, final KB entry marks "decommissioned <ts>", alerts pruned. Path: prompt "decommission X" → search/show/confirm flow (BACKLOG #25) → cascade delete (inventory + alerts) → final audit KB row written. Time: ~5 min. Acceptance: device is gone from inventory-mcp + dashboards, KB has a row with "decommissioned <date>" + reason.<br>19. **Backup verification automated** — outcome: every morning, customer knows if last night's backup ran and what's missing. Path: BACKLOG #6 backup cron running → agent queries backup log on demand → diff vs prior day. Time: ~5 min/wk ongoing. Acceptance: prompt "Did last night's backup run?" returns a 1-paragraph summary + diff vs prior day.<br>20. **KB accuracy review** — outcome: customer can review pending KB entries, bulk approve/dismiss. Path: KB trust ladder (K0 pending → K3 approved) → bulk review queue ships with K3 UI (BACKLOG #51). Time: ~10 min/wk ongoing. Acceptance: customer can open the K3 review queue, see N pending entries, approve or dismiss each with a click.<br><br>**Doc front-matter:**<br>• **"Setup required first"** callout linking to BACKLOG #52 Phase A steps (the first 4 use cases in Category A need only Phase A done; Categories B-E require Phase B minimum).<br>• **"Trust ladder"** reference so customers know which use cases are read-only vs which require confirmation (gated prompts per BACKLOG #52 Phase C5).<br>• Cross-ref to BACKLOG #52 success plan for the full phased rollout context.<br>• "How to use this catalogue" note: each entry's *Path* is the script; the *Acceptance signal* is the goal; the *Prerequisite* tells you when to skip and come back later.<br><br>**Acceptance criteria:**<br>• Doc lives at `~/AIAMSBS/docs/use-cases.md`, mirrored to OneDrive (verified via `rclone ls`).<br>• Each of the 20 entries has all 6 fields (Outcome / Path / Prerequisite / Time to achieve / Acceptance signal / Related) in consistent format throughout the doc.<br>• Each Path references specific config files or BACKLOG rows (e.g., "configure alertmanager webhook" → `config/alertmanager.yml` + BACKLOG #3).<br>• Use cases are *achievable today* on `.220` — meaning the BACKLOG rows they reference are either RESOLVED or explicitly future-work (in which case the use case is marked "requires future work" with a note pointing at the BACKLOG row).<br>• Doc opens with a "Setup required first" callout linking #52 Phase A.<br>• Cross-references between use cases (e.g., #5 alert→ticket depends on #6 notifications + #7 packet) use **entry numbers**, not just BACKLOG row numbers.<br>• The doc is referenced from the dashboard side-panel UI (text-only, no Grafana plugin: customer clicks a "?" icon → opens the use-case link in their KB / browser).<br><br>**Out of scope:**<br>• vSphere-only outcomes (BACKLOG #38 dependent) — added when #38 lands.<br>• SNMP-only outcomes (BACKLOG #37 dependent) — added when #37 lands.<br>• When those land, the catalogue grows to 25 entries.<br>• **A tuning row for this doc** — separate row, see #56.<br><br>**Related:** #52 (success plan — uses this catalogue at Phase B5), #30 (KB — use cases 8, 20), #3 + #46 (alerts — use cases 5, 6, 7, 9), #14 + #39 (inventory — use cases 1, 2, 16), #25 (delete — use case 18), #35 (multi-host — use cases 3, 4, 12), #44 (Alloy — use cases 2, 4, 11, 12), #31 (self-improving loop — use case 8), #6 (backup cron — use case 19).<br><br>**Correction log:** 2026-07-29 — Ryland reframed from "20 prompts" to "20 outcomes + paths". Original prompt-shape draft superseded.<br><br>**Backlogged 2026-07-29 per Telegram thread (corrected same day).** Kanban: `t_595c1144` (created same turn as this row, unassigned, ready). | Medium |
| 56 | Post-install success plan tuning (post first customer cohort) | The first draft of `docs/post-install-success-plan.md` (BACKLOG #52) shipped today (2026-07-29) by subagent — 8,167 words, 14 phase prompts, front matter + 2 appendices. Ryland's same-day review: "looks real good. It will need some tuning later." This row tracks the tuning cycle after the first customer cohort runs through it.<br><br>**What this is NOT:** a content rewrite. The current draft is structurally correct.<br><br>**What this IS:** iteration based on real customer feedback. After ≥3 customers run the plan (Phase A end-to-end minimum, ideally all three phases), capture:<br>• Which prompts worked verbatim vs needed rewording (ambiguous? too technical? too verbose?)<br>• Which prereqs were wrong/missing/wrongly ordered<br>• Which troubleshooting links dead-ended<br>• Which Phase steps took longer than estimated (or shorter)<br>• Customer questions that weren't covered<br><br>**Deliverable:** `docs/post-install-success-plan-v0.2.md` (the v1 doc stays at `docs/post-install-success-plan.md`, v0.2 lives alongside it as a sibling — customer uses whichever is current; rotate when v1 supersedes v0.2). Mirror to OneDrive same path as v1 (overwrites the prior version).<br><br>**Process:**<br>1. **Trigger:** wait for ≥3 customers to complete Phase A (or all 3 phases ideally). Customer feedback channel = the assistant chat (they tell the agent what didn't work); plus a structured feedback form if Ryland wants one.<br>2. **Collect:** feedback goes into a private notes file (NOT in repo, NOT in OneDrive — Ryland's local notes only).<br>3. **Produce:** dispatch a kanban card to `aiamsbs_dev` to produce v0.2 with tracked changes.<br>4. **Ship:** v0.2 ships with a "What changed since v1" appendix citing each customer feedback that drove a change (anonymized: "Customer A said prompt X was ambiguous because ...").<br>5. **Repeat** for v1.1, v1.2, etc.<br><br>**Acceptance criteria:**<br>• Triggered only after ≥3 customers complete Phase A (not before — premature optimization).<br>• Tracked changes in v0.2 cite specific customer feedback (anonymized as above).<br>• Phase time-budget appendix is updated based on actual times, not the optimistic v1 estimates.<br>• **No content drift:** v0.2 still maps 1:1 to the BACKLOG #52 row (no scope creep into "while we're here, let's add X" — those are v0.3 or v1 additions).<br>• Doc + OneDrive mirror updated atomically (no half-synced state).<br>• Backlog gets a `RESOLVED` marker + commit SHA when v0.2 ships, and a follow-up row opens if v0.3 is anticipated.<br><br>**Out of scope:**<br>• Tuning the use-cases catalogue (BACKLOG #53) — separate row if needed (likely yes, mirror this one).<br>• Tuning the topology view (BACKLOG #54) — separate row if needed.<br>• Tuning the docs viewer (BACKLOG #55) — separate row if needed.<br>• Adding new prompts to the doc — that's scope creep, goes in v0.3 or v1.<br>• KB-capture of customer-feedback events (could be a Mode C capture use case for BACKLOG #30, separate row).<br><br>**Related:** #52 (the parent row — this is its tuning cycle), #53 (sibling — likely needs a parallel tuning row eventually), #30 (KB — Mode C capture could record customer-feedback-to-tuning-cycles as KB events).<br><br>**Backlogged 2026-07-29 per Telegram thread (Ryland review note: "looks real good. It will need some tuning later.").** Kanban: `t_373ed476` (created same turn as this row, unassigned, ready). | Low |
| 58 | Reverse proxy stack (HTTP-only, Caddy, path-primary) | Sibling stack that exposes AIAMSBS services on a single TCP/80 port so the 1–5 IT staff customer persona doesn't memorize `:3000`/`:9090`/`:3100`/`:12345`/`:8001`/`:8002`. **Path-based routing is primary, hostname-based is secondary** — small-shop customers frequently have no resolvable DNS name, so `http://192.168.0.220/grafana/` must work bare-IP out of the box. Hostname routing (`grafana.lan` → same backend) is supported as a fallback for shops that DO have DNS, configured via the same Caddyfile.<br><br>**Caddy chosen over Traefik:** Traefik's auto-TLS advantage is moot (HTTP-only by design; BACKLOG #7 deferred) and Traefik's dynamic Docker-discovery advantage is moot (services are declared statically in `docker-compose.yml`); Traefik was previously removed from this repo (BACKLOG #34 "Traefik scrub").<br><br>**URL map (path-primary):**<br>• `http://HOST/` → Grafana (default landing)<br>• `http://HOST/grafana/` → Grafana (explicit)<br>• `http://HOST/prom/` → Prometheus :9090<br>• `http://HOST/loki/` → Loki :3100<br>• `http://HOST/alloy/` → Alloy :12345<br>• `http://HOST/kb/` → kb-mcp :8002 web UI<br>• `http://HOST/kb-mcp/` → kb-mcp :8002 MCP transport<br>• `http://HOST/inv/` → inventory-mcp :8001 MCP transport<br>• `http://HOST/hermes/` → Hermes Dashboard :9119<br>Plus hostname fallbacks (`grafana.lan`, `prometheus.lan`, `loki.lan`, `alloy.lan`, `hermes.lan`) routed to the same backends.<br><br>**Six things the Caddyfile must get right (backends stay on direct ports during rollout — additive, not replacement):**<br>1. **`rewrite` per backend** to strip the path prefix before proxying (e.g. `/grafana/*` → `/*` upstream). Without this, backends see bogus path prefixes and generate broken links.<br>2. **`X-Forwarded-Host` and `X-Forwarded-Proto` headers** on every proxy — backends use these to generate correct public URLs in their HTML responses.<br>3. **Grafana env overrides**: `GF_SERVER_ROOT_URL=http://192.168.0.220/grafana/`, `GF_SERVER_SERVE_FROM_SUB_PATH=true` — Grafana emits drilldown / share / datasource links that include its public prefix only when these are set.<br>4. **Hermes web rebuild with `base: '/hermes/'`** — Hermes ships as a Vite SPA whose built assets are referenced by absolute path. Set `base` in Vite config, rebuild during `deploy_reverse_proxy()`, restart the Hermes web server. Otherwise `http://HOST/hermes/assets/index-XYZ.js` 404s because the upstream serves it at `/assets/...` not `/hermes/assets/...`.<br>5. **kb-mcp SPA rebuild with `base: '/kb/'`** — same as Hermes; the bare HTML viewer references assets by absolute path.<br>6. **Catch-all routing**: a request to `http://HOST/` with no path match (and no hostname match) lands on Grafana as the default landing. Document this for the customer; don't silently 404.<br><br>**Bootstrap integration** — `bootstrap.sh` adds `deploy_reverse_proxy()` called after `deploy_main_stack` and before the auxiliary stacks. Function:<br>• Creates `reverse-proxy-stack/` sibling dir (compose + Caddyfile + admin API bound to `127.0.0.1:2019`).<br>• Joins the existing `monitoring` Docker network so backend DNS resolves (Caddy uses container DNS like `grafana:3000`, `prometheus:9090`).<br>• Runs the Hermes + kb-mcp rebuild steps above (Caddy side: `rewrite` strips prefix; backend side: `base` rebuild + restart).<br>• Idempotent — safe to re-run.<br><br>**Observability (project convention for every new service):**<br>• `prometheus.yml`: new `reverse_proxy_alive` blackbox scrape job probing `http://caddy:80/alive` (Caddyfile `/alive` returns `200 ok`).<br>• `health-check.json`: new 2×2 status tile for the Caddy container (per device convention).<br><br>**Acceptance criteria:**<br>• `curl http://192.168.0.220/grafana/` on a clean VM returns 200 (no DNS required).<br>• `curl http://192.168.0.220/grafana/api/health` returns `{"database":"ok"}` — proves prefix stripping works.<br>• `curl http://192.168.0.220/hermes/` returns 200 and the HTML references assets under `/hermes/assets/...` (Vite base rebuilt).<br>• `curl http://192.168.0.220/kb/ui/` returns 200 (kb-mcp SPA served behind prefix).<br>• `curl http://grafana.lan/` returns 200 (hostname fallback works for DNS-equipped shops).<br>• `/alive` blackbox probe shows `probe_success{job="reverse_proxy_alive"} == 1` in Prometheus.<br>• Health dashboard shows the new 2×2 tile green.<br>• Direct ports (`:3000`, `:9090`, etc.) still respond on the host — proxy is additive, not replacement.<br>• Bootstrap is idempotent — running `bash bootstrap.sh` on an already-provisioned VM leaves everything green and doesn't rebuild the image unnecessarily.<br><br>**Out of scope (separate rows):** TLS / HTTPS termination (BACKLOG #7 deferred, kept deferred), per-endpoint auth (oauth2-proxy in front of Caddy, future work), hostname-based cert provisioning (depends on BACKLOG #7).<br><br>**Backlogged 2026-08-02 per Telegram thread.** Plan (path-primary design): `.hermes/plans/2026-08-02_1430-caddy-reverse-proxy.md`. Kanban: not yet assigned — open after the kb-mcp / bootstrap fixes land. | Low |
| 59 | Ansible-based AIAMSBS install/management | Use Ansible to remotely install and manage AIAMSBS VM(s) across customer fleets. **Mechanism:** develop an Ansible role (or collection) that wraps the existing `bootstrap.sh` — same install behavior, but driven from a playbook + inventory file. Bootstrap's individual functions (`install_docker`, `install_hermes`, `deploy_main_stack`, etc.) become Ansible tasks; the shell script remains as the canonical bootstrap path. **Why:** small shops with 2-5 AIAMSBS instances (multi-site, MSP-style) currently have to `curl|bash` each VM individually; Ansible gives them inventory-driven, idempotent, centralized install/upgrade. **Scope:** (a) install new AIAMSBS VM from a control host; (b) upgrade existing AIAMSBS (re-runs are idempotent); (c) config push (`config/prometheus.yml`, dashboards, etc.); (d) hooks for the Windows fleet (links to #60). **Acceptance criteria:** (1) `ansible-playbook -i inventory site.yml` installs a fresh AIAMSBS VM end-to-end; (2) re-running on an installed VM is a no-op (idempotent); (3) working inventory example for a 2-VM HA setup; (4) playbook lint clean (`ansible-lint`). **Related:** #15 (Ansible container — single-VM scope, resolved), #23 (release + CI/CD pattern — playbook integrates here), #44 (Alloy on customer hosts — different target, related tooling). **Out of scope:** Ansible Tower / AWX (use stock `ansible-playbook`); Windows-specific Alloy automation (separate row #60). **Backlogged 2026-08-02 per Telegram thread.** | Medium |
| 60 | Windows fleet install/management (AIAMSBS agents) | Remote install + configuration of AIAMSBS components on Windows customer hosts. **Mechanism:** Ansible-for-Windows playbook using `winrm` (or PowerShell DSC, evaluated at implementation time) — wraps the manual Alloy install steps currently in `docs/multi-host-monitoring.md`. **Why:** Windows fleet install is manual per-host today (download MSI, edit config, open services.msc); AIAMSBS customers with 5+ Windows servers need automation. **Scope:** (a) bulk install Alloy on Windows fleet; (b) update Alloy config remotely (push new scrape targets, retention changes); (c) restart Alloy service after config update; (d) clean uninstall for offboarding. **Acceptance criteria:** (1) `ansible-playbook windows.yml -i win_inventory` installs Alloy on N Windows hosts in parallel; (2) post-install check: `Get-Service alloy` shows Running; (3) Alloy config endpoint reaches Loki/Prometheus within 60s of install; (4) uninstall playbook leaves the host clean. **Related:** #44 (Alloy migration step 2 — Windows River — this row automates the manual steps), #59 (Ansible AIAMSBS — same Ansible base). **Out of scope:** GPO-based deployment (different tooling, future row), macOS (use Linux playbook with adjustments), MSI signing / code-signing cert (separate prerequisite row). **Backlogged 2026-08-02 per Telegram thread.** | Medium |
| 61 | automation-stack (Ansible-backed OEM ops as Hermes MCP tools) | New sibling stack (alongside `kb-stack/`, `inventory-stack/`, `reverse-proxy-stack/`) that exposes Ansible-backed operations against customer devices — network gear (Cisco, Ubiquiti, Aruba, Fortinet, Palo Alto, Juniper), Windows servers, AD, Linux, vSphere — as Hermes MCP tools. The `it_admin` profile (BACKLOG #12) can then act on its 23 OEM skill files instead of being documentation-only. **Two containers, not one:** (1) `automation-mcp` (FastMCP server, port 8100) exposes `list_managed_hosts`, `get_host_details`, `list_playbooks`, `run_playbook`, `get_playbook_result`, `get_job_log`, `cancel_job`. (2) `automation-runner` (Ansible + all OEM collections baked in, no exposed port) executes jobs from the MCP queue. **Inventory source:** dynamic Ansible inventory script that calls `inventory-mcp:list_devices()` at runtime — no static inventory to maintain. **Security:** Ansible Vault at `/etc/ansible/vault.yml` (AES-256, key in `~/.hermes/.env`) for customer credentials; vault decrypted in runner container only at job start, never logged. Destructive ops require `confirm=True` per BACKLOG #25. Audit log of every job → Loki `from=automation`. **Observability (project convention):** blackbox `/alive` probe; 2×2 status tile in `health-check.json`; Grafana dashboard `aiamsbs-automation` (queue depth, success rate by vendor, recent failures). **Bootstrap integration:** `deploy_automation_stack()` called after `deploy_reverse_proxy()` (which is after `deploy_main_stack`). Registers `automation-mcp` in `it_admin` profile only — `default` profile has no access (read-only AIAMSBS view). **Open questions:** vault backend (file Vault for v1, swap to vaultwarden when #48-50 land?); OEM coverage (all 7 in IT_ADMIN for v1, or network+Windows first?); job queue (SQLite v1, Postgres for HA?); playbooks (own vs Galaxy roles). **Acceptance criteria:** (1) `automation-mcp` tools registered in `it_admin` profile; `default` profile has no automation tools; (2) `run_playbook` on a Cisco switch (`cisco/backup-running-config`) returns job_id, completes within 60s, log in Loki, artifact saved; (3) `run_playbook` on a Windows host (`windows/install-feature`) installs via WinRM; (4) `run_playbook` on vSphere (`vsphere/list-vms`) returns inventory; (5) destructive ops require `confirm=True`, otherwise return "confirmation required"; (6) 2×2 health tile green; blackbox probe `probe_success{job="automation_mcp_alive"} == 1`; (7) idempotent bootstrap re-runs. **Full architecture doc:** `onedrive:/obsidian_vaults/agent vault/AIAMSBS_RESEARCH/2026-08-02-automation-stack-architecture.md`. **Related:** #12 (IT_ADMIN profile — skills that get actions), #25 (confirmation pattern), #44 (Alloy migration — complementary), #48-50 (vaultwarden/bitwarden/mcp-server — future credential backend), #58 (reverse proxy — `/automation/` entry), #59 (Ansible base), #60 (Windows fleet). **Out of scope:** TLS termination on automation-mcp (#7 covers main path), `ansible.cfg` performance tuning, GPO deployment, MSI code-signing, macOS. **Backlogged 2026-08-02 per Telegram thread.** | High |
| 62 | Alternative Hermes Dashboard web UI (AIAMSBS-owned) | Replace the upstream Hermes Dashboard Vite/React SPA (currently served from the docs-site installer's managed Node build — the same build path BACKLOG #58 already requires us to fork for `base: '/hermes/'`) with an AIAMSBS-owned alternative. Today the upstream UI is generic Hermes — not AIAMSBS-flavored — and the AIAMSBS-specific workflows (review KB entries, dispatch automation playbooks, browse inventory) are all CLI-mediated via Hermes prompts. **Mechanism:** a thin static SPA (Svelte or similar, single `dist/` build, no SSR) shipped in-repo under a new `hermes-ui/` sibling dir. Talks to existing MCP servers (kb-mcp :8002, inventory-mcp :8001, automation-mcp :8100 when #61 lands) for AIAMSBS data; chat panel proxies to the upstream Hermes gateway for the agent itself. **Why:** (a) AIAMSBS owns the customer-facing surface end-to-end (branding, theming, audit trail of what changed); (b) eliminates the Vite/npm-build step that BACKLOG #58 requires — a static `dist/` deploys trivially behind the reverse proxy; (c) AIAMSBS-specific workflows become first-class UI pages instead of hidden behind prompts; (d) upstream Hermes Dashboard stays available for power users under `/hermes-classic/` (set in Caddyfile, no rebuild needed). **Scope (v1):** sections for KB pending review (links to kb-mcp), inventory list (links to inventory-mcp), automation recent jobs (links to automation-mcp, post-#61), chat panel (proxies to Hermes); reverse-proxy integration per BACKLOG #58 (lands at `/` as default). **Open questions:** SPA framework (Svelte vs Vue vs Solid vs React — pick at implementation); whether to consume MCP directly from the browser or via a thin server-side proxy (CORS, auth). **Acceptance criteria:** (1) `http://HOST/` lands on the AIAMSBS SPA; (2) KB / inventory / automation sections show live data via MCP; (3) chat panel round-trips to Hermes (it_admin profile); (4) upstream `hermes-classic` UI still accessible at `/hermes-classic/`; (5) static build in <30s; (6) reverse-proxy `/alive` probe unchanged; (7) bundle size <500KB gzipped (fits on a slow VM). **Related:** #12 (it_admin profile — drives chat-panel capability), #30 (kb-mcp — KB section data), #39 (inventory — inventory section data), #52 (post-install success plan — UI surfaces Phase A/B/C workflows), #55 (kb-mcp SPA — same pattern, smaller scope), #58 (reverse proxy — landing page decision), #61 (automation-stack — automation section data). **Out of scope:** full app rewrite of Hermes agent runtime (stays upstream; we only own the UI shell), mobile native app (later, separate row), SSR (v1 is static SPA only). **Backlogged 2026-08-02 per Telegram thread.** | High |

## Inventory UX (New Capability Track)

| # | Item | Description | Complexity |
|---|------|-------------|------------|
| 54 | Inventory topology view (Cytoscape / nodeGraph / flowcharting) | Today the customer can list devices, search, and run per-host dashboards (BACKLOG #14, #35), but there's no **single graph view** answering "what talks to what, where is everything, what depends on what." This is the **#1 visual feature homelab-hub (RaidOwl) has that AIAMSBS doesn't** — confirmed in the homelab-hub evaluation 2026-07-29.<br><br>**AIAMSBS v2.x scope** — this row is deferred to the AIAMSBS **v2.x** major release per Ryland decision 2026-07-29. The topology view ships as a built-in feature of v2.x, **not v1.x**. Two Grafana-native options to evaluate at v2.x planning time: Grafana's built-in `nodeGraph` panel, or the `vaduga-mapgl-panel` (Mapgl Node Graph) community plugin. Decision is made at v2.x scope-time, not v1.x pick-up. Do not scope this into the v1.x roadmap.<br><br>**Data source:** `inventory-mcp` `list_devices` (BACKLOG #14) + the relationships table that ships with it. Read-only on inventory data.<br><br>**Two Grafana-native options to evaluate at v2.x planning time (both shipped as built-in features of v2.x — pick the one that wins on the criteria below, or support both behind a flag):**<br><br>• **(a) Grafana built-in `nodeGraph` panel** — ships with Grafana, zero install. Renders nodes + edges from a datasource query (Prometheus / Loki / JSON datasource). Auto-layout (dagre-style). Limited interactivity beyond click-to-filter + drill-down. **Pros:** zero install cost for the customer, no license carry-through, ships with whatever Grafana version v2.x targets, no single-vendor dependency. **Cons:** limited styling customization, no multi-hop edge semantics, no per-fragment row data. **This is the "Grafana v2.x built-in" baseline** — what AIAMSBS v2.x would be without a community plugin.<br>• **(b) `vaduga-mapgl-panel` (Mapgl Node Graph)** — community plugin, Grafana marketplace id `vaduga-mapgl-panel`, repo github.com/vaduga/mapgl, **GPLv3**. **Dual-mode topology panel** — choose "Node Graph ortho" basemap for abstract auto-laid-out graph, or a real basemap for geo placement. Distinctive feature: **multi-hop edges as first-class primitive** (edges can pass through intermediate vertices; per-fragment row data retained). Single unified dataframe (Vertex A / Vertex B / Edge ID) works with any Grafana datasource including CSV. Threshold-driven node styling integrates with Grafana's threshold system — maps directly to AIAMSBS's per-host up/down coloring. **Active upstream** (commits within the last week; v2.8.0 two days ago at evaluation time). **Pros:** significantly more capable than (a), verified live on the demo dashboard at play.mapgl.org, dual-mode lets v2.x offer both abstract topology AND geo placement from one plugin. **Cons:** GPLv3 means AIAMSBS does NOT bundle or modify the plugin source — customer installs it from the Grafana marketplace after Grafana is up (one-line addition to `bootstrap.sh` to run `grafana cli plugins install vaduga-mapgl-panel`). Has a paid "Pro" tier; open-source core is sufficient for the topology use case. Single-maintainer (Vadim Pyatakov) — vendor-risk if upstream goes quiet. **Added 2026-07-29 after Ryland found the live demo at play.mapgl.org.**<br><br>**Pick criteria (decision deferred to v2.x planning time):**<br>• If the customer can install community plugins from the Grafana marketplace → choose (b) for capability, ship a fallback note that (a) is the no-plugin path.<br>• If the customer requires zero third-party plugins (e.g., air-gapped, strict supply-chain review) → choose (a).<br>• Default recommendation: **(b) is the primary; (a) is the documented fallback.** Both options work; the choice is operational, not technical.<br><br>**Other fallbacks (only if (a) AND (b) both fail to satisfy):**<br>• **(c) `algenty/grafana-flowcharting` plugin** (1,039★, MIT) — flowchart / draw.io-style with live data binding. Best for **hand-drawn topology maps** where the artist knows the layout. Doesn't auto-layout from a nodes/edges JSON.<br>• **(d) Custom Grafana panel plugin hosting Cytoscape** — full Cytoscape UX (force-directed, drag/pan/zoom, custom shapes, compound nodes). Highest effort, full UX parity with homelab-hub.<br>• **(e) Standalone Svelte + Cytoscape SPA + iframe / markdown-panel embed** — Cytoscape as a separate service on a sibling port, embedded in a Grafana panel via iframe. Decoupled, independent deploy.<br>• **(f) Grafana App (Scenes)** — newer Grafana 10+ pattern for full Svelte-based pages inside Grafana. Heaviest.<br><br>**Homelab-hub attribution:** this row was created after the homelab-hub evaluation 2026-07-29. The MIT-licensed Cytoscape design pattern is referenced; **no code is being vendored**. The relationship data model (storage → share, hardware → VM → app) was the inspiration; we re-derive it from inventory-mcp's existing schema. No import of homelab-hub DB, no port of homelab-hub code.<br><br>**Acceptance criteria:**<br>• A topology view exists at a stable URL on the AIAMSBS host (`http://<host>:XXXX/topology/`), reachable from the per-host dashboard's left nav<br>• Data backed by `inventory-mcp list_devices` (verified by smoke test: kill inventory-mcp → graph shows "no data")<br>• At least 2 layout modes available (or whatever the chosen plugin supports)<br>• Click a node → deep-link to the per-host dashboard with `$host` pre-set, OR to the kb-mcp UI for that device's KB entries<br>• Permission: read-only inventory data, no auth boundary changes<br>• Documented in `docs/post-install-success-plan.md` Phase B5 (BACKLOG #52) as use case "Show me my network topology"<br><br>**Out of scope (separate rows):** SNMP-driven topology discovery (#37 once landed), real-time alert overlay on topology (edges red on active alert), edit-in-graph (drag-to-add-relationship), vSphere cluster visualization (#38 once landed), geographic geo-map overlay (different visualization, different problem).<br><br>**Related:** #14 (inventory-mcp — data source), #35 (multi-host observability — sibling visualization), #52 (success plan — references from Phase B5), #53 (use cases — adds the topology use case).<br><br>**Backlogged 2026-07-29 per Telegram thread (post-homelab-hub evaluation).** Kanban: `t_11ecdd70` (created same turn as this row, unassigned, ready). | Medium |
| 55 | Docs/markdown viewer for kb-mcp (split-pane editor + live preview) | `kb-mcp` (BACKLOG #30) ships a bare-bones HTML viewer at `:8002/ui/` — functional but no editing UX, no live preview, no split-pane editor. homelab-hub (RaidOwl) has the **best-in-class markdown docs UX** in the homelab space: hierarchical tree on the left, ByteMD split-pane editor + live preview on the right, auto-save. MIT-licensed. This row ports that pattern to AIAMSBS's kb-mcp, keeping the MCP tool surface as the single source of truth.<br><br>**Deliverable:** a Svelte SPA served from `kb-mcp`'s existing `:8002/ui/` route, replacing the bare viewer. Three panes: tree (left), editor (middle, ByteMD), preview (right). Auto-save via debounced `kb_update` MCP calls. Trust-ladder promotion (K0 pending → K3 approved) exposed as inline buttons next to each entry.<br><br>**Components reused:**<br>• **ByteMD** (`bytemd`, MIT) for the editor + preview pane — same library homelab-hub uses<br>• Svelte 4 + Vite 5 (same stack as homelab-hub; the build artifacts ship inside the kb-mcp container)<br>• Pico CSS for styling (matches homelab-hub aesthetic, MIT)<br><br>**Data source:** `kb-mcp` `kb_search`, `kb_get`, `kb_update`, `kb_list`, `kb_delete` (BACKLOG #30 tools). Read + write through the **existing MCP tool surface** — never direct DB access. This guarantees the customer-facing UI and the agent face the same data, and Mode C capture (BACKLOG #30) keeps working unchanged.<br><br>**Acceptance criteria:**<br>• `kb-mcp:8002/ui/` renders a tree of KB entries on the left; click to open editor on the right<br>• Editor has split-pane with live markdown preview (ByteMD), supporting GitHub-flavored markdown + tables + code fences<br>• Auto-save on edit (debounced ~2s after typing stops) calls `kb_update` MCP tool; visible save-state indicator (idle / saving / saved)<br>• Trust-ladder promotion: an entry marked `status=pending` shows "Approve" / "Edit" / "Delete" buttons inline; promotion calls `kb_update` with the new status (K0 → K1 → ... → K3)<br>• All calls go through the MCP tool surface (verified by `kb-mcp` access logs showing tool invocations matching every UI action)<br>• Permissions: same as the current `kb-mcp` viewer (no additional auth boundary changes)<br>• Container size impact: +N MB for the Svelte build artifacts (verify with `docker images kb-mcp`); kb-mcp image stays single-architecture multi-platform like today<br><br>**Out of scope (separate rows):** Per-user edit history / soft-delete, attachment uploads (PDF/DOCX — already a follow-up in #50), collaborative editing (multi-user concurrent), KB schema versioning, search-result highlighting beyond simple substring match (kb-mcp's FTS5 + vector search is unchanged).<br><br>**Related:** #30 (kb-mcp — data + tool surface), #31 (KB self-correcting loop — surfaces corrections in this UI), #51 (KB docs+diagram — co-authored), #52 (success plan — referenced from KB workflow steps), #53 (use cases — KB-related entries reference this UI).<br><br>**Backlogged 2026-07-29 per Telegram thread (post-homelab-hub evaluation).** Kanban: `t_b624c591` (created same turn as this row, unassigned, ready). | Low |

## Pending Decisions

| # | Item | Description |
|---|------|-------------|
| D1 | Decide default agent purpose | Is `default` a coordinator (per #13), generic, customer-initiated routing, or something else? Blocks #13 + #14 sequencing and IT_ADMIN's routing logic. |

## Known Bugs

| # | Item | Description | Complexity |
|---|------|-------------|-----------|
| 57 | ~~kb-mcp /ui/ endpoint returns 404 (UI is missing from current kb-mcp build)~~ | **[RESOLVED — commit 085a071, 2026-07-30. Fix shipped to .220 as branch `feature/kb-mcp-ui-regression-fix-20260729`.]** Root cause: server.py used `mcp.run(transport='streamable-http')` with no HTML routes registered; FastMCP's transport owned port 8002 entirely. Fix: added 6 routes via `@mcp.custom_route()` (FastMCP ≥1.0 feature) — GET `/`, GET `/ui`, GET `/ui/` (HTML viewer, ~5.4KB single-file SPA), GET `/api/kb/list` (wraps `kb_list()`), GET `/api/kb/entries/{entry_id:int}` (direct SELECT — no `kb_get` MCP tool exists), PATCH `/api/kb/entries/{entry_id:int}` (wraps `kb_update()`). No direct DB access from the UI; same MCP tool functions are the sole data path. **Verified live on .220:** `curl /` → 200, `curl /ui` → 200, `curl /ui/` → 200, `curl /api/kb/list` → 200 (returns 2 existing entries), MCP transport still responding. Existing KB data preserved. **Next step (not in scope of #57):** user merges the feature branch to main; on .220: `git checkout main && git pull && git stash pop` to restore the stashed dirty files. **Unblocks #55** (Svelte + ByteMD upgrade) which was gated on the baseline existing. | — |
| 45 | Loki running multi-tenant despite single-tenant config comment | Loki serves **HTTP 401 "no org id"** for any push without `X-Scope-OrgID`, but the file comment says *"Local configuration (no auth, no persistence)"* and the design intent is single-tenant. **Discovered 2026-07-17** while bringing the Win11 Alloy host online (BACKLOG #44 pilot) — `loki.source.windowsevent` blocks fire from the Win11 box, the alloy service is up, `Test-NetConnection 192.168.0.220 -Port 3100` succeeds, but every push returns 401. Verified directly from `.220`: `curl -X POST http://192.168.0.220:3100/loki/api/v1/push` → **401**; same with `-H "X-Scope-OrgID: fake"` → **400** (accepted, just bad data).<br><br>**Root cause:** `config/loki.yml` does NOT actually set `auth_enabled: false` anywhere — only the file header comment claims it does. So Loki (k218 / 2.9-era) falls back to its compiled-in default, which on this build is multi-tenant. The metrics path doesn't need the header, which is why no one noticed until we added a Loki writer.<br><br>**Current workaround:** every Alloy `loki.write` block that points at the AIAMSBS Loki must include `headers = { "X-Scope-OrgID" = "<tenant>" }`. The Win11 Alloy now does this with `tenant-a` (matching what loki uses as default for queries from this stack).<br><br>**Proposed fix (DEFERRED — do not apply until Win11 Alloy pilot is verified end-to-end):**<br>• Add `auth_enabled: false` to `common:` (or top-level) in `config/loki.yml`. This makes single-tenant explicit and lets every writer drop the header.<br>• Verify which tenant (if any) other writers — `promtail`, `alloy-customer`, the AIAMSBS host `alloy` — currently use, so any header-bearing writer added later picks a consistent value (or drops it).<br>• Update `config/alloy.yml` + `config/alloy/customer-linux.river` + `customer-windows.river` to remove the `X-Scope-OrgID` header block (cleanup, not required).<br>• Re-test: `curl :3100/ready`, push a single dummy stream without header → expect 204.<br><br>**Why deferred:** Ryland's instruction 2026-07-17 — confirm the Win11 Alloy works as currently configured (with the header workaround) before changing the loki config. A misapplied `auth_enabled: false` flip while a pilot is mid-rollout would invalidate the verification baseline. Add to BACKLOG now; pick up after Win11 pilot verification (kanban `t_be84d9c7` series under #44). | Low |
| 21 | `mcp_servers` config format (Hermes bug) | **[RESOLVED — PR #7](https://github.com/rylanddufour/AIAMSBS/pull/7), merged 2026-06-27.** `bootstrap.sh` `register_inventory_mcp` now writes DICT format and is also called for `it_admin`. Hermes CLI's `tools_config.py:1365` still expects dict (option (b) — patch Hermes to handle both formats — remains open as a separate item if desired). | — |

---

## Completed
- [x] BACKLOG #8 — Service dependency health [WITHDRAWN 2026-07-21] — dependency-graph view never built; per-service health (#2) is sufficient.
- [x] BACKLOG #44 — Migrate customer hosts to Alloy [RESOLVED — PR #35 (commit 46b7c96)](https://github.com/rylanddufour/AIAMSBS/pull/35), 2026-07-19. All 11 in-scope steps landed; step 8 dropped (AIAMSBS doesn't install on customer devices).
- [x] BACKLOG #43 — Auto-discover linux_exporter / windows_exporter targets [RESOLVED — superseded by #44]. Alloy's `integrations/unix`/`integrations/windows` jobs replace the manual exporter target flow.
- [x] BACKLOG #45 — Loki multi-tenant config mismatch [RESOLVED — PR #30 (commit 89192b3)](https://github.com/rylanddufour/AIAMSBS/pull/30), 2026-07-17. `auth_enabled: false` set in `config/loki.yml`; X-Scope-OrgID workaround removed. Stale doc/comment references remain as historical context.
- [x] BACKLOG #A — Fix container metrics [RESOLVED — commit b5d0249](https://github.com/rylanddufour/AIAMSBS/commit/b5d0249). Alloy cAdvisor with host mounts; slimmed stack replaces node-exporter + cAdvisor.
- [x] BACKLOG #B — Add hostname label [RESOLVED — same as #10, commit 41e4119](https://github.com/rylanddufour/AIAMSBS/commit/41e4119). (Pre-req duplicate of #10.)
- [x] BACKLOG #10 — Add hostname label to Alloy metrics [RESOLVED — PR #29 (commit 41e4119)](https://github.com/rylanddufour/AIAMSBS/commit/41e4119), 2026-07-17.
- [x] BACKLOG #12 — Multi-OEM skill library [RESOLVED — commit 983a2ba](https://github.com/rylanddufour/AIAMSBS/commit/983a2ba), 2026-07-08. All 7 in-scope OEMs covered by 23 skill files in `profiles/it_admin/skills/`. SQL Server = future new-scope item, not a #12 gap.
- [x] BACKLOG #14 — Inventory MCP stack [RESOLVED — commit 38050fb](https://github.com/rylanddufour/AIAMSBS/commit/38050fb), 2026-06-27 (E2E verification). `inventory-stack/` subdir + FastMCP + nmap-discovery + IT_ADMIN/default registration.
- [x] BACKLOG #20 — IT_ADMIN Profile [RESOLVED — commit 983a2ba](https://github.com/rylanddufour/AIAMSBS/commit/983a2ba), 2026-07-08. `profiles/it_admin/SOUL.md` + 23 skills; `bootstrap.sh install_it_admin_profile_soul()` ships it.
- [x] BACKLOG #27 — AIAMSBS host logs in health dashboard [RESOLVED — covered by #2 (commit 7385d7b)](https://github.com/rylanddufour/AIAMSBS/commit/7385d7b), 2026-06-28. Live Logs panel shows host logs via `{source="aiamsbs_host"}`. **Caveat:** granular noise filter (priority=err, sshd/sudo substring) from the original spec was not built.
- [x] BACKLOG #42 — Inventory — Device Status dashboard: Polystat render broken [RESOLVED — workaround, commit a145a1b](https://github.com/rylanddufour/AIAMSBS/commit/a145a1b), 2026-07-10. Polystat abandoned; plain stat panel works.

- [x] BACKLOG #5 — Log retention config [RESOLVED — commit 68fe095](https://github.com/rylanddufour/AIAMSBS/commit/68fe095), 2026-07-15. **Fixes the broken PR #18 entry** — that PR added the config but the loki container was started with the image default `-config.file`, so our mounted `config/loki.yml` was silently ignored. `docker-compose.yml` now sets `command: -config.file=/etc/loki/local-config.yml`; `config/loki.yml` got 3 loki-3.2.0 schema compat fixes (`max_streams_matchers_per_query` rename, `allow_structured_metadata: false`, `compactor.delete_request_store: filesystem`). 90-day retention + compactor + analytics opt-out now actually enforced on .220.
- [x] BACKLOG #39 — Phase 2 Network Discovery (scheduled discovery + blackbox-from-inventory) [RESOLVED — commit 2ced874](https://github.com/rylanddufour/AIAMSBS/commit/2ced874), 2026-07-11. All 7 sub-items shipped + diagram update + Inventory Status dashboard. Deferred sub-items live on as #40, #41, #42.
- [x] BACKLOG #2 — Health check dashboard [RESOLVED — commit 7385d7b](https://github.com/rylanddufour/AIAMSBS/commit/7385d7b), 2026-06-28
- [x] BACKLOG #25 — Inventory `delete_device` tool + confirmation flow [RESOLVED — PR #10](https://github.com/rylanddufour/AIAMSBS/pull/10), 2026-06-27
- [x] BACKLOG #24 — Default profile MCP auto-loading [RESOLVED — PR #9](https://github.com/rylanddufour/AIAMSBS/pull/9), 2026-06-27
- [x] BACKLOG #22 — Skill safety gates (agent self-modification hardening) [RESOLVED — PR #8](https://github.com/rylanddufour/AIAMSBS/pull/8), 2026-06-27. `bootstrap.sh` `configure_skill_safety()` sets `skills.write_approval: true` (skill writes staged to `/skills pending` for review) and `skills.guard_agent_created: true` (agent-created skills scanned for exfiltration/persistence/destructive patterns). Closes the gap that `~/.hermes/profiles/*/skills/*.md` is not in `file_tools._SENSITIVE_PATH_PREFIXES` — out of the box the agent could edit its own skills.
- [x] BACKLOG #21 — `mcp_servers` config format (Hermes bug) [RESOLVED — PR #7](https://github.com/rylanddufour/AIAMSBS/pull/7), 2026-06-27
- [x] Config-as-code deployment (v2.1)
- [x] Docker Compose stack definition
- [x] Alloy metrics + logs collection
- [x] Hermes WebUI in container
- [x] Hermes logs collection (~/.hermes/logs)
- [x] Remote access (0.0.0.0 binding)